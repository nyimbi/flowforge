// Submit a Building Permit Application step component — generated by flowforge JTBD generator.
// Re-run the generator to regenerate; do not edit by hand.
import * as React from "react";

export interface PermitIntakeStepProps {
	instanceId: string;
	state: string;
	onEvent: (event: string, payload?: Record<string, unknown>) => Promise<void> | void;
}

const FIELDS: ReadonlyArray<{ id: string; label: string; required: boolean; pii: boolean }> = [
	{ id: "owner_name", label: "Property Owner Name", required: true, pii: true },
	{ id: "owner_email", label: "Owner Email", required: true, pii: true },
	{ id: "owner_phone", label: "Owner Phone", required: true, pii: true },
	{ id: "site_address", label: "Construction Site Address", required: true, pii: false },
	{ id: "parcel_number", label: "Parcel / APN Number", required: true, pii: false },
	{ id: "permit_type", label: "Permit Type", required: true, pii: false },
	{ id: "project_description", label: "Project Description", required: true, pii: false },
	{ id: "estimated_cost", label: "Estimated Construction Cost", required: true, pii: false },
	{ id: "contractor_license", label: "Contractor License Number", required: false, pii: false },
];

/**
 * Minimal review step for the permit_intake workflow. Hosts wrap this with
 * the project-wide auth/tenant context — keeping the component dumb so
 * generated code stays portable across Next.js and plain Vite apps.
 */
export function PermitIntakeStep(props: PermitIntakeStepProps): React.ReactElement {
	const [busy, setBusy] = React.useState(false);

	const fire = React.useCallback(
		async (event: string) => {
			if (busy) return;
			setBusy(true);
			try {
				await props.onEvent(event, {});
			} finally {
				setBusy(false);
			}
		},
		[busy, props],
	);

	return (
		<section data-testid="permit_intake-step" aria-labelledby="permit_intake-step-title">
			<header>
				<h2 id="permit_intake-step-title">Submit a Building Permit Application</h2>
				<p>State: <code>{props.state}</code></p>
				<p>Instance: <code>{props.instanceId}</code></p>
			</header>
			<dl>
				{FIELDS.map((field) => (
					<React.Fragment key={field.id}>
						<dt>{field.label}{field.required ? " *" : ""}{field.pii ? " (PII)" : ""}</dt>
						<dd>—</dd>
					</React.Fragment>
				))}
			</dl>
			<footer>
				<button type="button" disabled={busy} onClick={() => fire("submit")}>
					Submit
				</button>
				<button type="button" disabled={busy} onClick={() => fire("approve")}>
					Approve
				</button>
			</footer>
		</section>
	);
}
