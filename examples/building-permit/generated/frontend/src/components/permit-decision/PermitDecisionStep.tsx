// Approve or Deny Building Permit step component — generated by flowforge JTBD generator.
// Re-run the generator to regenerate; do not edit by hand.
import * as React from "react";

export interface PermitDecisionStepProps {
	instanceId: string;
	state: string;
	onEvent: (event: string, payload?: Record<string, unknown>) => Promise<void> | void;
	/** Explicitly exposes workflow state / instance diagnostics for developer tools. */
	developerMode?: boolean;
}

const FIELDS: ReadonlyArray<{ id: string; label: string; required: boolean; pii: boolean }> = [
	{ id: "decision", label: "Decision", required: true, pii: false },
	{ id: "conditions", label: "Conditions of Approval", required: false, pii: false },
	{ id: "denial_findings", label: "Denial Findings", required: false, pii: false },
	{ id: "expiry_date", label: "Permit Expiry Date", required: false, pii: false },
];

/**
 * Minimal review step for the permit_decision workflow. Hosts wrap this with
 * the project-wide auth/tenant context — keeping the component dumb so
 * generated code stays portable across Next.js and plain Vite apps.
 */
export function PermitDecisionStep(props: PermitDecisionStepProps): React.ReactElement {
	const [busy, setBusy] = React.useState(false);

	const fire = React.useCallback(
		async (event: string) => {
			if (busy) return;
			setBusy(true);
			try {
				await props.onEvent(event, { instance_id: props.instanceId });
			} finally {
				setBusy(false);
			}
		},
		[busy, props],
	);

	return (
		<section
			data-flowforge-form-root
			data-testid="permit_decision-step"
			aria-labelledby="permit_decision-step-title"
		>
			<header>
				<h2 id="permit_decision-step-title">Approve or Deny Building Permit</h2>
				{props.developerMode ? (
					<dl aria-label="Workflow diagnostics">
						<dt>State</dt>
						<dd><code>{props.state}</code></dd>
						<dt>Instance</dt>
						<dd><code>{props.instanceId}</code></dd>
					</dl>
				) : null}
			</header>
			<dl>
				{FIELDS.map((field) => (
					<React.Fragment key={field.id}>
						<dt>{field.label}{field.required ? " *" : ""}{field.pii ? " (PII)" : ""}</dt>
						<dd>—</dd>
					</React.Fragment>
				))}
			</dl>
			<footer>
				<button type="button" disabled={busy} onClick={() => fire("submit")}>
					Submit
				</button>
				<button type="button" disabled={busy} onClick={() => fire("approve")}>
					Approve
				</button>
			</footer>
		</section>
	);
}
