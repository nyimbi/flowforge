// Next.js app-router page for the Issue the Building Permit Certificate workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { PermitIssuanceStep } from "@/components/permit-issuance/PermitIssuanceStep";

export const dynamic = "force-dynamic";

export default function PermitIssuancePage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/permit-issuance/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Issue the Building Permit Certificate</h1>
			<PermitIssuanceStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
