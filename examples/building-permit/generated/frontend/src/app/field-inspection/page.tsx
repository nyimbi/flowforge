// Next.js app-router page for the Conduct Field Inspection workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { FieldInspectionStep } from "@/components/field-inspection/FieldInspectionStep";

export const dynamic = "force-dynamic";

export default function FieldInspectionPage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/field-inspection/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Conduct Field Inspection</h1>
			<FieldInspectionStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
