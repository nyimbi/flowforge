// Next.js app-router page for the Approve or Deny Building Permit workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { PermitDecisionStep } from "@/components/permit-decision/PermitDecisionStep";

export const dynamic = "force-dynamic";

export default function PermitDecisionPage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/permit-decision/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Approve or Deny Building Permit</h1>
			<PermitDecisionStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
