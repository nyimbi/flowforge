// Next.js app-router page for the Submit a Building Permit Application workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { PermitIntakeStep } from "@/components/permit-intake/PermitIntakeStep";

export const dynamic = "force-dynamic";

export default function PermitIntakePage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/permit-intake/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Submit a Building Permit Application</h1>
			<PermitIntakeStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
