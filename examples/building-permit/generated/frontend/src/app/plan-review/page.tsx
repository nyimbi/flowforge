// Next.js app-router page for the Review Building Plans for Code Compliance workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { PlanReviewStep } from "@/components/plan-review/PlanReviewStep";

export const dynamic = "force-dynamic";

export default function PlanReviewPage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/plan-review/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Review Building Plans for Code Compliance</h1>
			<PlanReviewStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
