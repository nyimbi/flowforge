"""Inbound email parser + reply-subject route catalog for building-permit.

Generated by flowforge JTBD generator — do not edit by hand. The
catalog is closed at generation time so the W3 conformance gate can
assert each entry mirrors a JTBD declared in the bundle.
"""

from __future__ import annotations

import re
from dataclasses import dataclass


@dataclass(frozen=True)
class FieldSpec:
	"""One ``data_capture`` field exposed as an inbound `key: value` line."""

	id: str
	label: str
	required: bool


@dataclass(frozen=True)
class RouteSpec:
	"""One reply-subject route: ``[<jtbd>:<event>:<instance_id>]``."""

	jtbd_id: str
	jtbd_title: str
	url_segment: str
	event: str
	subject_token: str
	fields: tuple[FieldSpec, ...]


REPLY_ROUTES: tuple[RouteSpec, ...] = (
	RouteSpec(
		jtbd_id="field_inspection",
		jtbd_title="Conduct Field Inspection",
		url_segment="field-inspection",
		event="approve",
		subject_token="field_inspection:approve",
		fields=(
			FieldSpec(id="correction_items", label="Correction Items", required=False),
			FieldSpec(id="inspection_date", label="Inspection Date", required=True),
			FieldSpec(id="inspection_type", label="Inspection Type", required=True),
			FieldSpec(id="inspector_notes", label="Inspector Field Notes", required=True),
			FieldSpec(id="pass_fail", label="Result", required=True),
		),
	),
	RouteSpec(
		jtbd_id="field_inspection",
		jtbd_title="Conduct Field Inspection",
		url_segment="field-inspection",
		event="escalate",
		subject_token="field_inspection:escalate",
		fields=(
			FieldSpec(id="correction_items", label="Correction Items", required=False),
			FieldSpec(id="inspection_date", label="Inspection Date", required=True),
			FieldSpec(id="inspection_type", label="Inspection Type", required=True),
			FieldSpec(id="inspector_notes", label="Inspector Field Notes", required=True),
			FieldSpec(id="pass_fail", label="Result", required=True),
		),
	),
	RouteSpec(
		jtbd_id="field_inspection",
		jtbd_title="Conduct Field Inspection",
		url_segment="field-inspection",
		event="reject",
		subject_token="field_inspection:reject",
		fields=(
			FieldSpec(id="correction_items", label="Correction Items", required=False),
			FieldSpec(id="inspection_date", label="Inspection Date", required=True),
			FieldSpec(id="inspection_type", label="Inspection Type", required=True),
			FieldSpec(id="inspector_notes", label="Inspector Field Notes", required=True),
			FieldSpec(id="pass_fail", label="Result", required=True),
		),
	),
	RouteSpec(
		jtbd_id="field_inspection",
		jtbd_title="Conduct Field Inspection",
		url_segment="field-inspection",
		event="submit",
		subject_token="field_inspection:submit",
		fields=(
			FieldSpec(id="correction_items", label="Correction Items", required=False),
			FieldSpec(id="inspection_date", label="Inspection Date", required=True),
			FieldSpec(id="inspection_type", label="Inspection Type", required=True),
			FieldSpec(id="inspector_notes", label="Inspector Field Notes", required=True),
			FieldSpec(id="pass_fail", label="Result", required=True),
		),
	),
	RouteSpec(
		jtbd_id="permit_decision",
		jtbd_title="Approve or Deny Building Permit",
		url_segment="permit-decision",
		event="approve",
		subject_token="permit_decision:approve",
		fields=(
			FieldSpec(id="conditions", label="Conditions of Approval", required=False),
			FieldSpec(id="decision", label="Decision", required=True),
			FieldSpec(id="denial_findings", label="Denial Findings", required=False),
			FieldSpec(id="expiry_date", label="Permit Expiry Date", required=False),
		),
	),
	RouteSpec(
		jtbd_id="permit_decision",
		jtbd_title="Approve or Deny Building Permit",
		url_segment="permit-decision",
		event="reject",
		subject_token="permit_decision:reject",
		fields=(
			FieldSpec(id="conditions", label="Conditions of Approval", required=False),
			FieldSpec(id="decision", label="Decision", required=True),
			FieldSpec(id="denial_findings", label="Denial Findings", required=False),
			FieldSpec(id="expiry_date", label="Permit Expiry Date", required=False),
		),
	),
	RouteSpec(
		jtbd_id="permit_decision",
		jtbd_title="Approve or Deny Building Permit",
		url_segment="permit-decision",
		event="submit",
		subject_token="permit_decision:submit",
		fields=(
			FieldSpec(id="conditions", label="Conditions of Approval", required=False),
			FieldSpec(id="decision", label="Decision", required=True),
			FieldSpec(id="denial_findings", label="Denial Findings", required=False),
			FieldSpec(id="expiry_date", label="Permit Expiry Date", required=False),
		),
	),
	RouteSpec(
		jtbd_id="permit_intake",
		jtbd_title="Submit a Building Permit Application",
		url_segment="permit-intake",
		event="approve",
		subject_token="permit_intake:approve",
		fields=(
			FieldSpec(id="contractor_license", label="Contractor License Number", required=False),
			FieldSpec(id="estimated_cost", label="Estimated Construction Cost", required=True),
			FieldSpec(id="owner_email", label="Owner Email", required=True),
			FieldSpec(id="owner_name", label="Property Owner Name", required=True),
			FieldSpec(id="owner_phone", label="Owner Phone", required=True),
			FieldSpec(id="parcel_number", label="Parcel / APN Number", required=True),
			FieldSpec(id="permit_type", label="Permit Type", required=True),
			FieldSpec(id="project_description", label="Project Description", required=True),
			FieldSpec(id="site_address", label="Construction Site Address", required=True),
		),
	),
	RouteSpec(
		jtbd_id="permit_intake",
		jtbd_title="Submit a Building Permit Application",
		url_segment="permit-intake",
		event="submit",
		subject_token="permit_intake:submit",
		fields=(
			FieldSpec(id="contractor_license", label="Contractor License Number", required=False),
			FieldSpec(id="estimated_cost", label="Estimated Construction Cost", required=True),
			FieldSpec(id="owner_email", label="Owner Email", required=True),
			FieldSpec(id="owner_name", label="Property Owner Name", required=True),
			FieldSpec(id="owner_phone", label="Owner Phone", required=True),
			FieldSpec(id="parcel_number", label="Parcel / APN Number", required=True),
			FieldSpec(id="permit_type", label="Permit Type", required=True),
			FieldSpec(id="project_description", label="Project Description", required=True),
			FieldSpec(id="site_address", label="Construction Site Address", required=True),
		),
	),
	RouteSpec(
		jtbd_id="permit_issuance",
		jtbd_title="Issue the Building Permit Certificate",
		url_segment="permit-issuance",
		event="approve",
		subject_token="permit_issuance:approve",
		fields=(
			FieldSpec(id="clerk_signature", label="Clerk Signature", required=True),
			FieldSpec(id="fee_amount", label="Total Fee Collected", required=True),
			FieldSpec(id="issued_date", label="Issued Date", required=True),
			FieldSpec(id="permit_number", label="Permit Number", required=True),
			FieldSpec(id="receipt_number", label="Payment Receipt Number", required=True),
		),
	),
	RouteSpec(
		jtbd_id="permit_issuance",
		jtbd_title="Issue the Building Permit Certificate",
		url_segment="permit-issuance",
		event="submit",
		subject_token="permit_issuance:submit",
		fields=(
			FieldSpec(id="clerk_signature", label="Clerk Signature", required=True),
			FieldSpec(id="fee_amount", label="Total Fee Collected", required=True),
			FieldSpec(id="issued_date", label="Issued Date", required=True),
			FieldSpec(id="permit_number", label="Permit Number", required=True),
			FieldSpec(id="receipt_number", label="Payment Receipt Number", required=True),
		),
	),
	RouteSpec(
		jtbd_id="plan_review",
		jtbd_title="Review Building Plans for Code Compliance",
		url_segment="plan-review",
		event="approve",
		subject_token="plan_review:approve",
		fields=(
			FieldSpec(id="code_sections", label="Applicable Code Sections", required=False),
			FieldSpec(id="review_notes", label="Review Notes", required=True),
			FieldSpec(id="review_outcome", label="Review Outcome", required=True),
		),
	),
	RouteSpec(
		jtbd_id="plan_review",
		jtbd_title="Review Building Plans for Code Compliance",
		url_segment="plan-review",
		event="request_more_info",
		subject_token="plan_review:request_more_info",
		fields=(
			FieldSpec(id="code_sections", label="Applicable Code Sections", required=False),
			FieldSpec(id="review_notes", label="Review Notes", required=True),
			FieldSpec(id="review_outcome", label="Review Outcome", required=True),
		),
	),
	RouteSpec(
		jtbd_id="plan_review",
		jtbd_title="Review Building Plans for Code Compliance",
		url_segment="plan-review",
		event="submit",
		subject_token="plan_review:submit",
		fields=(
			FieldSpec(id="code_sections", label="Applicable Code Sections", required=False),
			FieldSpec(id="review_notes", label="Review Notes", required=True),
			FieldSpec(id="review_outcome", label="Review Outcome", required=True),
		),
	),
)


# Subject-suffix grammar:  [<jtbd>:<event>:<instance_id>]
# Captured groups: 1 = jtbd, 2 = event, 3 = instance_id.
_SUBJECT_RE = re.compile(r"\[([a-z][a-z0-9_]*):([a-z][a-z0-9_]*):([^\]]+)\]")


def parse_subject(subject: str) -> tuple[str, str, str] | None:
	"""Return ``(jtbd, event, instance_id)`` if *subject* carries the
	flowforge reply-subject suffix, else ``None``."""

	assert isinstance(subject, str), "subject must be a string"
	match = _SUBJECT_RE.search(subject)
	if match is None:
		return None
	return match.group(1), match.group(2), match.group(3)


def parse_body(body: str) -> dict[str, str]:
	"""Parse a ``key: value`` reply body into a payload dict.

	The grammar is intentionally simple — one assignment per line, the
	first ``:`` is the separator, leading/trailing whitespace stripped.
	Lines without ``:`` are dropped silently. Quoted values stay quoted;
	the host's payload-validation layer takes the JSON cast.
	"""

	assert isinstance(body, str), "body must be a string"
	out: dict[str, str] = {}
	for line in body.splitlines():
		if ":" not in line:
			continue
		key, _, value = line.partition(":")
		key = key.strip()
		value = value.strip()
		if not key:
			continue
		out[key] = value
	return out


def lookup(jtbd: str, event: str) -> RouteSpec | None:
	"""Return the catalog entry matching ``(jtbd, event)`` or ``None``."""

	assert isinstance(jtbd, str), "jtbd must be a string"
	assert isinstance(event, str), "event must be a string"
	for route in REPLY_ROUTES:
		if route.jtbd_id == jtbd and route.event == event:
			return route
	return None
