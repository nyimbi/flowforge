"""Outbound email templates for building-permit.

Generated by flowforge JTBD generator — do not edit by hand. One
template per audit topic the bundle aggregates; hosts substitute
``{tenant_id}`` / ``{instance_id}`` / ``{actor}`` / ``{reply_token}``
placeholders at send time.

The reply grammar suffix ``[<jtbd>:<event>:<instance_id>]`` is appended
by the host's notification adapter (the audit topic alone does not
imply which event the recipient should fire on reply — that's a
business decision the host owns).
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class EmailTemplate:
	"""One outbound notification template, keyed by audit topic."""

	topic: str
	subject_template: str
	body_template: str


# Closed catalog of email templates, sorted by topic so the file is
# deterministic across regens. Hosts call ``template_for(topic)`` to
# look one up before rendering placeholders.
EMAIL_TEMPLATES: dict[str, EmailTemplate] = {
	"field_inspection.approved": EmailTemplate(
		topic="field_inspection.approved",
		subject_template="[field_inspection.approved] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The field_inspection.approved event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"field_inspection.escalated": EmailTemplate(
		topic="field_inspection.escalated",
		subject_template="[field_inspection.escalated] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The field_inspection.escalated event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"field_inspection.failed_inspection_rejected": EmailTemplate(
		topic="field_inspection.failed_inspection_rejected",
		subject_template="[field_inspection.failed_inspection_rejected] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The field_inspection.failed_inspection_rejected event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"field_inspection.submitted": EmailTemplate(
		topic="field_inspection.submitted",
		subject_template="[field_inspection.submitted] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The field_inspection.submitted event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"permit_decision.approved": EmailTemplate(
		topic="permit_decision.approved",
		subject_template="[permit_decision.approved] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The permit_decision.approved event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"permit_decision.denied_rejected": EmailTemplate(
		topic="permit_decision.denied_rejected",
		subject_template="[permit_decision.denied_rejected] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The permit_decision.denied_rejected event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"permit_decision.submitted": EmailTemplate(
		topic="permit_decision.submitted",
		subject_template="[permit_decision.submitted] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The permit_decision.submitted event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"permit_intake.approved": EmailTemplate(
		topic="permit_intake.approved",
		subject_template="[permit_intake.approved] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The permit_intake.approved event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"permit_intake.submitted": EmailTemplate(
		topic="permit_intake.submitted",
		subject_template="[permit_intake.submitted] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The permit_intake.submitted event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"permit_issuance.approved": EmailTemplate(
		topic="permit_issuance.approved",
		subject_template="[permit_issuance.approved] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The permit_issuance.approved event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"permit_issuance.submitted": EmailTemplate(
		topic="permit_issuance.submitted",
		subject_template="[permit_issuance.submitted] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The permit_issuance.submitted event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"plan_review.approved": EmailTemplate(
		topic="plan_review.approved",
		subject_template="[plan_review.approved] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The plan_review.approved event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"plan_review.corrections_needed_returned": EmailTemplate(
		topic="plan_review.corrections_needed_returned",
		subject_template="[plan_review.corrections_needed_returned] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The plan_review.corrections_needed_returned event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"plan_review.major_variance_required": EmailTemplate(
		topic="plan_review.major_variance_required",
		subject_template="[plan_review.major_variance_required] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The plan_review.major_variance_required event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
	"plan_review.submitted": EmailTemplate(
		topic="plan_review.submitted",
		subject_template="[plan_review.submitted] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The plan_review.submitted event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- building-permit\n"
		),
	),
}


def template_for(topic: str) -> EmailTemplate | None:
	"""Return the template registered for *topic* (or ``None``)."""

	assert isinstance(topic, str), "topic must be a string"
	return EMAIL_TEMPLATES.get(topic)
