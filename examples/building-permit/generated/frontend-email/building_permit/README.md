# building-permit — email-driven adapter shell

Generated by flowforge JTBD generator (item 9 of `docs/improvements.md`,
W3 of `docs/v0.3.0-engineering-plan.md`). Re-run the generator to
update; do not edit by hand.

## What this is

A shell-only inbound/outbound email adapter for the `building-permit`
bundle. The generator emits:

* `inbound.py` — RFC-5322 parser ABC and the reply-to-fire-event
  workflow.
* `templates.py` — outbound notification email templates keyed by
  audit topic.
* `router.py` — reply-subject grammar walker that maps inbound
  messages onto a POST against `/<url-segment>/events`.

## What this is *not*

The generator emits the **routing skeleton only**. Hosts wire the
concrete IMAP / SMTP / SES / SendGrid bridge themselves:

* IMAP credentials and inbox polling cadence;
* SMTP / SES / SendGrid outbound transport;
* DKIM / SPF / DMARC verification (defence in depth — never trust the
  reply-subject token alone);
* identity bridging from the `From:` header to the bundle's tenant +
  user model.

That intentional cut keeps the bundle deterministic and lets each host
pick its own email runtime.

## Operation source

Every reply-subject route is pinned to the same `openapi.yaml` the W1
generator emits at the bundle root (`../../openapi.yaml` relative
to this package). The adapter does **not** re-derive operations from
the bundle.

## Reply-subject grammar

Outbound notifications carry a subject suffix of the form::

	[<jtbd>:<event>:<instance_id>]

Reply-to-fire dispatches an event by parsing the suffix and POSTing to
the matching backend route. The closed catalog of `<jtbd>:<event>`
tokens lives in `inbound.py`:

* `field_inspection:approve` — Conduct Field Inspection (approve)
* `field_inspection:escalate` — Conduct Field Inspection (escalate)
* `field_inspection:reject` — Conduct Field Inspection (reject)
* `field_inspection:submit` — Conduct Field Inspection (submit)
* `permit_decision:approve` — Approve or Deny Building Permit (approve)
* `permit_decision:reject` — Approve or Deny Building Permit (reject)
* `permit_decision:submit` — Approve or Deny Building Permit (submit)
* `permit_intake:approve` — Submit a Building Permit Application (approve)
* `permit_intake:submit` — Submit a Building Permit Application (submit)
* `permit_issuance:approve` — Issue the Building Permit Certificate (approve)
* `permit_issuance:submit` — Issue the Building Permit Certificate (submit)
* `plan_review:approve` — Review Building Plans for Code Compliance (approve)
* `plan_review:request_more_info` — Review Building Plans for Code Compliance (request_more_info)
* `plan_review:submit` — Review Building Plans for Code Compliance (submit)

The reply body becomes the JSON `payload`; the parser accepts a simple
`key: value` line grammar so manual replies stay readable.

## Outbound templates

The bundle's notification adapter posts emails through the templates
in `templates.py`. One template per audit topic the bundle aggregates:

* `field_inspection.approved`
* `field_inspection.escalated`
* `field_inspection.failed_inspection_rejected`
* `field_inspection.submitted`
* `permit_decision.approved`
* `permit_decision.denied_rejected`
* `permit_decision.submitted`
* `permit_intake.approved`
* `permit_intake.submitted`
* `permit_issuance.approved`
* `permit_issuance.submitted`
* `plan_review.approved`
* `plan_review.corrections_needed_returned`
* `plan_review.major_variance_required`
* `plan_review.submitted`

## Wiring

Hosts subclass `EmailAdapter` from `router.py` and implement two
hooks:

* `verify_envelope(message)` — DKIM/SPF/DMARC and From-header
  validation.
* `resolve_principal(message)` — bridge the sender's email to the
  bundle's tenant + user model.

Then call `dispatch(message)` from your IMAP poller. The router takes
care of subject parsing and POSTing to the right backend endpoint.

## Installation

```bash
cd frontend-email/building_permit
pip install -e .
```
