"""SQLAlchemy model for Submit a Building Permit Application — generated by flowforge JTBD generator.

Do not edit by hand; rerun ``flowforge new`` (or the generator unit
tests) to regenerate. Edits should happen in the JTBD bundle.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any

from sqlalchemy import (
	Boolean,
	Date,
	DateTime,
	Integer,
	Numeric,
	String,
	Text,
)
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
	"""Project-local declarative base; alembic upgrades target this metadata."""


class PermitIntake(Base):
	__tablename__ = "permit_intake"

	id: Mapped[str] = mapped_column(String(36), primary_key=True)
	tenant_id: Mapped[str | None] = mapped_column(String(36), nullable=True, index=True)
	state: Mapped[str] = mapped_column(String(64), nullable=False, default="intake")
	created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	owner_name: Mapped[Any] = mapped_column(String(512), nullable=False)
	owner_email: Mapped[Any] = mapped_column(String(320), nullable=False)
	owner_phone: Mapped[Any] = mapped_column(String(40), nullable=False)
	site_address: Mapped[Any] = mapped_column(String(512), nullable=False)
	parcel_number: Mapped[Any] = mapped_column(String(512), nullable=False)
	permit_type: Mapped[Any] = mapped_column(String(64), nullable=False)
	project_description: Mapped[Any] = mapped_column(Text(), nullable=False)
	estimated_cost: Mapped[Any] = mapped_column(Numeric(18, 2), nullable=False)
	contractor_license: Mapped[Any] = mapped_column(String(512), nullable=True)

	def __repr__(self) -> str:  # pragma: no cover - cosmetic
		return f"<PermitIntake id={self.id!r} state={self.state!r}>"
