"""SQLAlchemy model for Approve or Deny Building Permit — generated by flowforge JTBD generator.

Do not edit by hand; rerun ``flowforge new`` (or the generator unit
tests) to regenerate. Edits should happen in the JTBD bundle.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any

from sqlalchemy import (
	Boolean,
	Date,
	DateTime,
	Integer,
	Numeric,
	String,
	Text,
)
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
	"""Project-local declarative base; alembic upgrades target this metadata."""


class PermitDecision(Base):
	__tablename__ = "permit_decision"

	id: Mapped[str] = mapped_column(String(36), primary_key=True)
	tenant_id: Mapped[str | None] = mapped_column(String(36), nullable=True, index=True)
	state: Mapped[str] = mapped_column(String(64), nullable=False, default="intake")
	created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	decision: Mapped[Any] = mapped_column(String(64), nullable=False)
	conditions: Mapped[Any] = mapped_column(Text(), nullable=True)
	denial_findings: Mapped[Any] = mapped_column(Text(), nullable=True)
	expiry_date: Mapped[Any] = mapped_column(Date(), nullable=True)

	def __repr__(self) -> str:  # pragma: no cover - cosmetic
		return f"<PermitDecision id={self.id!r} state={self.state!r}>"
