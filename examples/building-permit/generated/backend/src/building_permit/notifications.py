"""Cross-bundle notifications router config for building-permit.

Generated by flowforge JTBD generator. Each entry maps a workflow
trigger to a (channel, audience) pair the host's notify adapter uses
to render + dispatch.
"""

from __future__ import annotations

from typing import TypedDict


class NotificationRule(TypedDict):
	trigger: str
	channel: str
	audience: str


NOTIFICATIONS: tuple[NotificationRule, ...] = (
	NotificationRule(trigger="approved", channel="email", audience="applicant"),
	NotificationRule(trigger="rejected", channel="email", audience="applicant"),
	NotificationRule(trigger="sla_breach", channel="in_app", audience="chief_building_official"),
	NotificationRule(trigger="sla_warn", channel="in_app", audience="chief_building_official"),
	NotificationRule(trigger="sla_warn", channel="in_app", audience="plan_reviewer"),
	NotificationRule(trigger="state_enter", channel="email", audience="applicant"),
)
