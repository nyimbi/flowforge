"""Alembic migration: create ``permit_intake`` for Submit a Building Permit Application.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "0cf990fae22e"
down_revision = None
branch_labels = ("building_permit_permit_intake",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"permit_intake",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("owner_name", sa.String(512), nullable=False),
		sa.Column("owner_email", sa.String(320), nullable=False),
		sa.Column("owner_phone", sa.String(40), nullable=False),
		sa.Column("site_address", sa.String(512), nullable=False),
		sa.Column("parcel_number", sa.String(512), nullable=False),
		sa.Column("permit_type", sa.String(64), nullable=False),
		sa.Column("project_description", sa.Text(), nullable=False),
		sa.Column("estimated_cost", sa.Numeric(18, 2), nullable=False),
		sa.Column("contractor_license", sa.String(512), nullable=True),
	)
	op.create_index(
		"ix_permit_intake_tenant_state",
		"permit_intake",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_permit_intake_tenant_state", table_name="permit_intake")
	op.drop_table("permit_intake")
