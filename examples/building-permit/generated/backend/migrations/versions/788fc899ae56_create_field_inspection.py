"""Alembic migration: create ``field_inspection`` for Conduct Field Inspection.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "788fc899ae56"
down_revision = None
branch_labels = ("building_permit_field_inspection",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"field_inspection",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("inspection_type", sa.String(64), nullable=False),
		sa.Column("inspection_date", sa.Date(), nullable=False),
		sa.Column("inspector_notes", sa.Text(), nullable=False),
		sa.Column("pass_fail", sa.String(64), nullable=False),
		sa.Column("correction_items", sa.Text(), nullable=True),
	)
	op.create_index(
		"ix_field_inspection_tenant_state",
		"field_inspection",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_field_inspection_tenant_state", table_name="field_inspection")
	op.drop_table("field_inspection")
