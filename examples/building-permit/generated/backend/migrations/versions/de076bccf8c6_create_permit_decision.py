"""Alembic migration: create ``permit_decision`` for Approve or Deny Building Permit.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "de076bccf8c6"
down_revision = None
branch_labels = ("building_permit_permit_decision",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"permit_decision",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("decision", sa.String(64), nullable=False),
		sa.Column("conditions", sa.Text(), nullable=True),
		sa.Column("denial_findings", sa.Text(), nullable=True),
		sa.Column("expiry_date", sa.Date(), nullable=True),
	)
	op.create_index(
		"ix_permit_decision_tenant_state",
		"permit_decision",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_permit_decision_tenant_state", table_name="permit_decision")
	op.drop_table("permit_decision")
