"""Alembic migration: create ``plan_review`` for Review Building Plans for Code Compliance.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "cfdd2cdc03cd"
down_revision = None
branch_labels = ("building_permit_plan_review",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"plan_review",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("review_notes", sa.Text(), nullable=False),
		sa.Column("code_sections", sa.String(512), nullable=True),
		sa.Column("review_outcome", sa.String(64), nullable=False),
	)
	op.create_index(
		"ix_plan_review_tenant_state",
		"plan_review",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_plan_review_tenant_state", table_name="plan_review")
	op.drop_table("plan_review")
