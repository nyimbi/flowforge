"""Alembic migration: create ``permit_issuance`` for Issue the Building Permit Certificate.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "202cbbc4fe13"
down_revision = None
branch_labels = ("building_permit_permit_issuance",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"permit_issuance",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("permit_number", sa.String(512), nullable=False),
		sa.Column("issued_date", sa.Date(), nullable=False),
		sa.Column("clerk_signature", sa.String(128), nullable=False),
		sa.Column("fee_amount", sa.Numeric(18, 2), nullable=False),
		sa.Column("receipt_number", sa.String(512), nullable=False),
	)
	op.create_index(
		"ix_permit_issuance_tenant_state",
		"permit_issuance",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_permit_issuance_tenant_state", table_name="permit_issuance")
	op.drop_table("permit_issuance")
