"""Typer CLI entry point for building-permit.

Generated by flowforge JTBD generator — do not edit by hand. Each JTBD
becomes a Typer subapp; each transition event becomes a command under
that subapp. The command tree mirrors the bundle's ``openapi.yaml``
(W1, item 8) one-to-one.

Run ``building-permit-cli --help`` to enumerate.
"""

from __future__ import annotations

import json
from typing import Any, Optional

import typer

from . import client


app = typer.Typer(
	help="Typer CLI client for building-permit — fires workflow events into the bundle's FastAPI backend.",
	no_args_is_help=True,
)

field_inspection_app = typer.Typer(
	help="Conduct Field Inspection",
	no_args_is_help=True,
)
app.add_typer(field_inspection_app, name="field-inspection")
permit_decision_app = typer.Typer(
	help="Approve or Deny Building Permit",
	no_args_is_help=True,
)
app.add_typer(permit_decision_app, name="permit-decision")
permit_intake_app = typer.Typer(
	help="Submit a Building Permit Application",
	no_args_is_help=True,
)
app.add_typer(permit_intake_app, name="permit-intake")
permit_issuance_app = typer.Typer(
	help="Issue the Building Permit Certificate",
	no_args_is_help=True,
)
app.add_typer(permit_issuance_app, name="permit-issuance")
plan_review_app = typer.Typer(
	help="Review Building Plans for Code Compliance",
	no_args_is_help=True,
)
app.add_typer(plan_review_app, name="plan-review")


@field_inspection_app.command("approve")
def field_inspection_approve(
	correction_items: Optional[str] = typer.Option(None, help="Correction Items"),
	inspection_date: str = typer.Option(..., help="Inspection Date (required)"),
	inspection_type: str = typer.Option(..., help="Inspection Type (required)"),
	inspector_notes: str = typer.Option(..., help="Inspector Field Notes (required)"),
	pass_fail: str = typer.Option(..., help="Result (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/field-inspection/events`` for Conduct Field Inspection."""

	payload: dict[str, Any] = {}
	if correction_items is not None:
		payload["correction_items"] = correction_items
	payload["inspection_date"] = inspection_date
	payload["inspection_type"] = inspection_type
	payload["inspector_notes"] = inspector_notes
	payload["pass_fail"] = pass_fail
	result = client.fire_sync(base_url, "field-inspection", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@field_inspection_app.command("escalate")
def field_inspection_escalate(
	correction_items: Optional[str] = typer.Option(None, help="Correction Items"),
	inspection_date: str = typer.Option(..., help="Inspection Date (required)"),
	inspection_type: str = typer.Option(..., help="Inspection Type (required)"),
	inspector_notes: str = typer.Option(..., help="Inspector Field Notes (required)"),
	pass_fail: str = typer.Option(..., help="Result (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``escalate`` against ``/field-inspection/events`` for Conduct Field Inspection."""

	payload: dict[str, Any] = {}
	if correction_items is not None:
		payload["correction_items"] = correction_items
	payload["inspection_date"] = inspection_date
	payload["inspection_type"] = inspection_type
	payload["inspector_notes"] = inspector_notes
	payload["pass_fail"] = pass_fail
	result = client.fire_sync(base_url, "field-inspection", "escalate", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@field_inspection_app.command("reject")
def field_inspection_reject(
	correction_items: Optional[str] = typer.Option(None, help="Correction Items"),
	inspection_date: str = typer.Option(..., help="Inspection Date (required)"),
	inspection_type: str = typer.Option(..., help="Inspection Type (required)"),
	inspector_notes: str = typer.Option(..., help="Inspector Field Notes (required)"),
	pass_fail: str = typer.Option(..., help="Result (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``reject`` against ``/field-inspection/events`` for Conduct Field Inspection."""

	payload: dict[str, Any] = {}
	if correction_items is not None:
		payload["correction_items"] = correction_items
	payload["inspection_date"] = inspection_date
	payload["inspection_type"] = inspection_type
	payload["inspector_notes"] = inspector_notes
	payload["pass_fail"] = pass_fail
	result = client.fire_sync(base_url, "field-inspection", "reject", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@field_inspection_app.command("submit")
def field_inspection_submit(
	correction_items: Optional[str] = typer.Option(None, help="Correction Items"),
	inspection_date: str = typer.Option(..., help="Inspection Date (required)"),
	inspection_type: str = typer.Option(..., help="Inspection Type (required)"),
	inspector_notes: str = typer.Option(..., help="Inspector Field Notes (required)"),
	pass_fail: str = typer.Option(..., help="Result (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/field-inspection/events`` for Conduct Field Inspection."""

	payload: dict[str, Any] = {}
	if correction_items is not None:
		payload["correction_items"] = correction_items
	payload["inspection_date"] = inspection_date
	payload["inspection_type"] = inspection_type
	payload["inspector_notes"] = inspector_notes
	payload["pass_fail"] = pass_fail
	result = client.fire_sync(base_url, "field-inspection", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@permit_decision_app.command("approve")
def permit_decision_approve(
	conditions: Optional[str] = typer.Option(None, help="Conditions of Approval"),
	decision: str = typer.Option(..., help="Decision (required)"),
	denial_findings: Optional[str] = typer.Option(None, help="Denial Findings"),
	expiry_date: Optional[str] = typer.Option(None, help="Permit Expiry Date"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/permit-decision/events`` for Approve or Deny Building Permit."""

	payload: dict[str, Any] = {}
	if conditions is not None:
		payload["conditions"] = conditions
	payload["decision"] = decision
	if denial_findings is not None:
		payload["denial_findings"] = denial_findings
	if expiry_date is not None:
		payload["expiry_date"] = expiry_date
	result = client.fire_sync(base_url, "permit-decision", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@permit_decision_app.command("reject")
def permit_decision_reject(
	conditions: Optional[str] = typer.Option(None, help="Conditions of Approval"),
	decision: str = typer.Option(..., help="Decision (required)"),
	denial_findings: Optional[str] = typer.Option(None, help="Denial Findings"),
	expiry_date: Optional[str] = typer.Option(None, help="Permit Expiry Date"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``reject`` against ``/permit-decision/events`` for Approve or Deny Building Permit."""

	payload: dict[str, Any] = {}
	if conditions is not None:
		payload["conditions"] = conditions
	payload["decision"] = decision
	if denial_findings is not None:
		payload["denial_findings"] = denial_findings
	if expiry_date is not None:
		payload["expiry_date"] = expiry_date
	result = client.fire_sync(base_url, "permit-decision", "reject", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@permit_decision_app.command("submit")
def permit_decision_submit(
	conditions: Optional[str] = typer.Option(None, help="Conditions of Approval"),
	decision: str = typer.Option(..., help="Decision (required)"),
	denial_findings: Optional[str] = typer.Option(None, help="Denial Findings"),
	expiry_date: Optional[str] = typer.Option(None, help="Permit Expiry Date"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/permit-decision/events`` for Approve or Deny Building Permit."""

	payload: dict[str, Any] = {}
	if conditions is not None:
		payload["conditions"] = conditions
	payload["decision"] = decision
	if denial_findings is not None:
		payload["denial_findings"] = denial_findings
	if expiry_date is not None:
		payload["expiry_date"] = expiry_date
	result = client.fire_sync(base_url, "permit-decision", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@permit_intake_app.command("approve")
def permit_intake_approve(
	contractor_license: Optional[str] = typer.Option(None, help="Contractor License Number"),
	estimated_cost: float = typer.Option(..., help="Estimated Construction Cost (required)"),
	owner_email: str = typer.Option(..., help="Owner Email (required)"),
	owner_name: str = typer.Option(..., help="Property Owner Name (required)"),
	owner_phone: str = typer.Option(..., help="Owner Phone (required)"),
	parcel_number: str = typer.Option(..., help="Parcel / APN Number (required)"),
	permit_type: str = typer.Option(..., help="Permit Type (required)"),
	project_description: str = typer.Option(..., help="Project Description (required)"),
	site_address: str = typer.Option(..., help="Construction Site Address (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/permit-intake/events`` for Submit a Building Permit Application."""

	payload: dict[str, Any] = {}
	if contractor_license is not None:
		payload["contractor_license"] = contractor_license
	payload["estimated_cost"] = estimated_cost
	payload["owner_email"] = owner_email
	payload["owner_name"] = owner_name
	payload["owner_phone"] = owner_phone
	payload["parcel_number"] = parcel_number
	payload["permit_type"] = permit_type
	payload["project_description"] = project_description
	payload["site_address"] = site_address
	result = client.fire_sync(base_url, "permit-intake", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@permit_intake_app.command("submit")
def permit_intake_submit(
	contractor_license: Optional[str] = typer.Option(None, help="Contractor License Number"),
	estimated_cost: float = typer.Option(..., help="Estimated Construction Cost (required)"),
	owner_email: str = typer.Option(..., help="Owner Email (required)"),
	owner_name: str = typer.Option(..., help="Property Owner Name (required)"),
	owner_phone: str = typer.Option(..., help="Owner Phone (required)"),
	parcel_number: str = typer.Option(..., help="Parcel / APN Number (required)"),
	permit_type: str = typer.Option(..., help="Permit Type (required)"),
	project_description: str = typer.Option(..., help="Project Description (required)"),
	site_address: str = typer.Option(..., help="Construction Site Address (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/permit-intake/events`` for Submit a Building Permit Application."""

	payload: dict[str, Any] = {}
	if contractor_license is not None:
		payload["contractor_license"] = contractor_license
	payload["estimated_cost"] = estimated_cost
	payload["owner_email"] = owner_email
	payload["owner_name"] = owner_name
	payload["owner_phone"] = owner_phone
	payload["parcel_number"] = parcel_number
	payload["permit_type"] = permit_type
	payload["project_description"] = project_description
	payload["site_address"] = site_address
	result = client.fire_sync(base_url, "permit-intake", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@permit_issuance_app.command("approve")
def permit_issuance_approve(
	clerk_signature: str = typer.Option(..., help="Clerk Signature (required)"),
	fee_amount: float = typer.Option(..., help="Total Fee Collected (required)"),
	issued_date: str = typer.Option(..., help="Issued Date (required)"),
	permit_number: str = typer.Option(..., help="Permit Number (required)"),
	receipt_number: str = typer.Option(..., help="Payment Receipt Number (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/permit-issuance/events`` for Issue the Building Permit Certificate."""

	payload: dict[str, Any] = {}
	payload["clerk_signature"] = clerk_signature
	payload["fee_amount"] = fee_amount
	payload["issued_date"] = issued_date
	payload["permit_number"] = permit_number
	payload["receipt_number"] = receipt_number
	result = client.fire_sync(base_url, "permit-issuance", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@permit_issuance_app.command("submit")
def permit_issuance_submit(
	clerk_signature: str = typer.Option(..., help="Clerk Signature (required)"),
	fee_amount: float = typer.Option(..., help="Total Fee Collected (required)"),
	issued_date: str = typer.Option(..., help="Issued Date (required)"),
	permit_number: str = typer.Option(..., help="Permit Number (required)"),
	receipt_number: str = typer.Option(..., help="Payment Receipt Number (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/permit-issuance/events`` for Issue the Building Permit Certificate."""

	payload: dict[str, Any] = {}
	payload["clerk_signature"] = clerk_signature
	payload["fee_amount"] = fee_amount
	payload["issued_date"] = issued_date
	payload["permit_number"] = permit_number
	payload["receipt_number"] = receipt_number
	result = client.fire_sync(base_url, "permit-issuance", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@plan_review_app.command("approve")
def plan_review_approve(
	code_sections: Optional[str] = typer.Option(None, help="Applicable Code Sections"),
	review_notes: str = typer.Option(..., help="Review Notes (required)"),
	review_outcome: str = typer.Option(..., help="Review Outcome (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/plan-review/events`` for Review Building Plans for Code Compliance."""

	payload: dict[str, Any] = {}
	if code_sections is not None:
		payload["code_sections"] = code_sections
	payload["review_notes"] = review_notes
	payload["review_outcome"] = review_outcome
	result = client.fire_sync(base_url, "plan-review", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@plan_review_app.command("request-more-info")
def plan_review_request_more_info(
	code_sections: Optional[str] = typer.Option(None, help="Applicable Code Sections"),
	review_notes: str = typer.Option(..., help="Review Notes (required)"),
	review_outcome: str = typer.Option(..., help="Review Outcome (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``request_more_info`` against ``/plan-review/events`` for Review Building Plans for Code Compliance."""

	payload: dict[str, Any] = {}
	if code_sections is not None:
		payload["code_sections"] = code_sections
	payload["review_notes"] = review_notes
	payload["review_outcome"] = review_outcome
	result = client.fire_sync(base_url, "plan-review", "request_more_info", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@plan_review_app.command("submit")
def plan_review_submit(
	code_sections: Optional[str] = typer.Option(None, help="Applicable Code Sections"),
	review_notes: str = typer.Option(..., help="Review Notes (required)"),
	review_outcome: str = typer.Option(..., help="Review Outcome (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/plan-review/events`` for Review Building Plans for Code Compliance."""

	payload: dict[str, Any] = {}
	if code_sections is not None:
		payload["code_sections"] = code_sections
	payload["review_notes"] = review_notes
	payload["review_outcome"] = review_outcome
	result = client.fire_sync(base_url, "plan-review", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


if __name__ == "__main__":
	app()
