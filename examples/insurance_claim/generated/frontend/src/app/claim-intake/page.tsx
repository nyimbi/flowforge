// Next.js app-router page for the File an insurance claim (FNOL) workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { ClaimIntakeStep } from "@/components/claim-intake/ClaimIntakeStep";

export const dynamic = "force-dynamic";

export default function ClaimIntakePage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/claim-intake/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>File an insurance claim (FNOL)</h1>
			<ClaimIntakeStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
