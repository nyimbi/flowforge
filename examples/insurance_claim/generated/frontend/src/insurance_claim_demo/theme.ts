// TypeScript theme module for insurance-claim-demo.
// Generated by flowforge JTBD generator (v0.3.0 W3 / item 18).
//
// Re-exports the design tokens as `var(--name)` references so React /
// styled-components / inline `style` attributes pick up the live CSS
// variable rather than baking a literal hex into the bundle. The
// `defaults` block carries the at-generation literal values for SSR /
// node-side consumers that cannot read CSS variables.

export type DesignDensity = "compact" | "comfortable";

export interface DesignTokens {
	colorPrimary: string;
	colorAccent: string;
	fontFamily: string;
	density: DesignDensity;
	densityPadding: string;
	radiusScale: string;
	radiusSm: string;
	radiusMd: string;
	radiusLg: string;
}

/**
 * Live design tokens — every field is a CSS `var(--…)` reference. Pass
 * any of these into a React `style` attribute and the value tracks the
 * CSS variable defined in `design_tokens.css`.
 */
export const tokens: DesignTokens = {
	colorPrimary: "var(--color-primary)",
	colorAccent: "var(--color-accent)",
	fontFamily: "var(--font-family)",
	density: "compact",
	densityPadding: "var(--density-padding)",
	radiusScale: "var(--radius-scale)",
	radiusSm: "var(--radius-sm)",
	radiusMd: "var(--radius-md)",
	radiusLg: "var(--radius-lg)",
};

/**
 * Literal values pinned at generation time. SSR / node consumers that
 * cannot read CSS variables read the same numbers the CSS file is
 * configured with. A re-run of `flowforge jtbd-generate` is the only
 * way to refresh these.
 */
export const defaults = {
	colorPrimary: "#0f766e",
	colorAccent: "#f59e0b",
	fontFamily: ""IBM Plex Sans", system-ui, sans-serif",
	density: "compact" as DesignDensity,
	radiusScale: 1.5,
} as const;
