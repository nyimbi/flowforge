// Tailwind theme config for insurance-claim-demo.
// Generated by flowforge JTBD generator (v0.3.0 W3 / item 18).
//
// Extend mode (not full override) so authors keep Tailwind's default
// palette + spacing scale and only the design-token overrides flow
// through. Colour utilities resolve through CSS variables defined in
// `design_tokens.css`, so a token swap re-themes utilities without a
// rebuild of Tailwind itself.
import type { Config } from "tailwindcss";

const config: Config = {
	content: [
		"./src/**/*.{ts,tsx,js,jsx}",
		"./app/**/*.{ts,tsx,js,jsx}",
		"./pages/**/*.{ts,tsx,js,jsx}",
	],
	theme: {
		extend: {
			colors: {
				primary: "var(--color-primary)",
				accent: "var(--color-accent)",
			},
			fontFamily: {
				sans: ["var(--font-family)"],
			},
			borderRadius: {
				sm: "var(--radius-sm)",
				md: "var(--radius-md)",
				lg: "var(--radius-lg)",
			},
		},
	},
	plugins: [],
};

export default config;
