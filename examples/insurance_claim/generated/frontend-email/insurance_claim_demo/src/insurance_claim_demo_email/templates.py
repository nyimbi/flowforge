"""Outbound email templates for insurance-claim-demo.

Generated by flowforge JTBD generator — do not edit by hand. One
template per audit topic the bundle aggregates; hosts substitute
``{tenant_id}`` / ``{instance_id}`` / ``{actor}`` / ``{reply_token}``
placeholders at send time.

The reply grammar suffix ``[<jtbd>:<event>:<instance_id>]`` is appended
by the host's notification adapter (the audit topic alone does not
imply which event the recipient should fire on reply — that's a
business decision the host owns).
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class EmailTemplate:
	"""One outbound notification template, keyed by audit topic."""

	topic: str
	subject_template: str
	body_template: str


# Closed catalog of email templates, sorted by topic so the file is
# deterministic across regens. Hosts call ``template_for(topic)`` to
# look one up before rendering placeholders.
EMAIL_TEMPLATES: dict[str, EmailTemplate] = {
	"claim_intake.approved": EmailTemplate(
		topic="claim_intake.approved",
		subject_template="[claim_intake.approved] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The claim_intake.approved event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- insurance-claim-demo\n"
		),
	),
	"claim_intake.escalated": EmailTemplate(
		topic="claim_intake.escalated",
		subject_template="[claim_intake.escalated] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The claim_intake.escalated event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- insurance-claim-demo\n"
		),
	),
	"claim_intake.lapsed_rejected": EmailTemplate(
		topic="claim_intake.lapsed_rejected",
		subject_template="[claim_intake.lapsed_rejected] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The claim_intake.lapsed_rejected event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- insurance-claim-demo\n"
		),
	),
	"claim_intake.large_loss": EmailTemplate(
		topic="claim_intake.large_loss",
		subject_template="[claim_intake.large_loss] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The claim_intake.large_loss event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- insurance-claim-demo\n"
		),
	),
	"claim_intake.submitted": EmailTemplate(
		topic="claim_intake.submitted",
		subject_template="[claim_intake.submitted] instance {instance_id} {reply_token}",
		body_template=(
			"Hello,\n\n"
			"The claim_intake.submitted event fired on instance {instance_id} "
			"(tenant {tenant_id}) by {actor}.\n\n"
			"Reply with the appropriate event token in the subject suffix "
			"to advance the workflow.\n\n"
			"-- insurance-claim-demo\n"
		),
	),
}


def template_for(topic: str) -> EmailTemplate | None:
	"""Return the template registered for *topic* (or ``None``)."""

	assert isinstance(topic, str), "topic must be a string"
	return EMAIL_TEMPLATES.get(topic)
