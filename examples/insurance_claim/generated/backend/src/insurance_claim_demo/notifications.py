"""Cross-bundle notifications router config for insurance-claim-demo.

Generated by flowforge JTBD generator. Each entry maps a workflow
trigger to a (channel, audience) pair the host's notify adapter uses
to render + dispatch.
"""

from __future__ import annotations

from typing import TypedDict


class NotificationRule(TypedDict):
	trigger: str
	channel: str
	audience: str


NOTIFICATIONS: tuple[NotificationRule, ...] = (
	NotificationRule(trigger="approved", channel="email", audience="claimant"),
	NotificationRule(trigger="rejected", channel="email", audience="claimant"),
	NotificationRule(trigger="sla_breach", channel="slack", audience="supervisor"),
	NotificationRule(trigger="sla_warn", channel="slack", audience="adjuster"),
	NotificationRule(trigger="state_enter", channel="email", audience="claimant"),
)
