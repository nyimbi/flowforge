"""Alembic migration: create ``claim_intake`` for File an insurance claim (FNOL).

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "2a43cfa86685"
down_revision = None
branch_labels = ("insurance_claim_demo_claim_intake",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"claim_intake",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("claimant_name", sa.String(512), nullable=False),
		sa.Column("policy_number", sa.String(512), nullable=False),
		sa.Column("loss_date", sa.Date(), nullable=False),
		sa.Column("loss_amount", sa.Numeric(18, 2), nullable=False),
		sa.Column("loss_description", sa.Text(), nullable=False),
		sa.Column("contact_email", sa.String(320), nullable=False),
		sa.Column("contact_phone", sa.String(40), nullable=True),
	)
	op.create_index(
		"ix_claim_intake_tenant_state",
		"claim_intake",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_claim_intake_tenant_state", table_name="claim_intake")
	op.drop_table("claim_intake")
