"""Alembic migration: create ``screen_candidate`` for Screen a candidate.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "9eb8ce261db4"
down_revision = None
branch_labels = ("hiring_pipeline_screen_candidate",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"screen_candidate",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("screen_score", sa.Numeric(), nullable=False),
		sa.Column("screen_notes", sa.Text(), nullable=False),
		sa.Column("years_exp", sa.Numeric(), nullable=False),
		sa.Column("salary_expect", sa.Numeric(18, 2), nullable=True),
		sa.Column("availability", sa.Date(), nullable=True),
	)
	op.create_index(
		"ix_screen_candidate_tenant_state",
		"screen_candidate",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_screen_candidate_tenant_state", table_name="screen_candidate")
	op.drop_table("screen_candidate")
