"""Alembic migration: create ``source_candidate`` for Source a candidate.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "abbc71f2a663"
down_revision = None
branch_labels = ("hiring_pipeline_source_candidate",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"source_candidate",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("candidate_name", sa.String(512), nullable=False),
		sa.Column("candidate_email", sa.String(320), nullable=False),
		sa.Column("candidate_phone", sa.String(40), nullable=True),
		sa.Column("role_title", sa.String(512), nullable=False),
		sa.Column("source_channel", sa.String(64), nullable=False),
		sa.Column("resume", sa.String(256), nullable=True),
		sa.Column("notes", sa.Text(), nullable=True),
	)
	op.create_index(
		"ix_source_candidate_tenant_state",
		"source_candidate",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_source_candidate_tenant_state", table_name="source_candidate")
	op.drop_table("source_candidate")
