"""Alembic migration: create ``complete_hire`` for Complete hire and onboard.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "eed4d2014f48"
down_revision = None
branch_labels = ("hiring_pipeline_complete_hire",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"complete_hire",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("acceptance_date", sa.Date(), nullable=False),
		sa.Column("legal_name", sa.String(512), nullable=False),
		sa.Column("national_id_ref", sa.String(512), nullable=False),
		sa.Column("home_address", sa.String(512), nullable=False),
		sa.Column("emergency_contact", sa.String(512), nullable=False),
		sa.Column("signed_contract", sa.String(128), nullable=False),
		sa.Column("bank_account_ref", sa.String(512), nullable=False),
	)
	op.create_index(
		"ix_complete_hire_tenant_state",
		"complete_hire",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_complete_hire_tenant_state", table_name="complete_hire")
	op.drop_table("complete_hire")
