"""Alembic migration: create ``extend_offer`` for Extend an offer.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "963812ac66d2"
down_revision = None
branch_labels = ("hiring_pipeline_extend_offer",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"extend_offer",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="intake"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("base_salary", sa.Numeric(18, 2), nullable=False),
		sa.Column("bonus_target", sa.Numeric(18, 2), nullable=True),
		sa.Column("start_date", sa.Date(), nullable=False),
		sa.Column("offer_expiry", sa.Date(), nullable=False),
		sa.Column("offer_letter", sa.String(256), nullable=False),
		sa.Column("equity_units", sa.Numeric(), nullable=True),
	)
	op.create_index(
		"ix_extend_offer_tenant_state",
		"extend_offer",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_extend_offer_tenant_state", table_name="extend_offer")
	op.drop_table("extend_offer")
