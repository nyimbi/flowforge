"""SQLAlchemy model for Complete hire and onboard — generated by flowforge JTBD generator.

Do not edit by hand; rerun ``flowforge new`` (or the generator unit
tests) to regenerate. Edits should happen in the JTBD bundle.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any

from sqlalchemy import (
	Boolean,
	Date,
	DateTime,
	Integer,
	Numeric,
	String,
	Text,
)
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
	"""Project-local declarative base; alembic upgrades target this metadata."""


class CompleteHire(Base):
	__tablename__ = "complete_hire"

	id: Mapped[str] = mapped_column(String(36), primary_key=True)
	tenant_id: Mapped[str | None] = mapped_column(String(36), nullable=True, index=True)
	state: Mapped[str] = mapped_column(String(64), nullable=False, default="intake")
	created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	acceptance_date: Mapped[Any] = mapped_column(Date(), nullable=False)
	legal_name: Mapped[Any] = mapped_column(String(512), nullable=False)
	national_id_ref: Mapped[Any] = mapped_column(String(512), nullable=False)
	home_address: Mapped[Any] = mapped_column(String(512), nullable=False)
	emergency_contact: Mapped[Any] = mapped_column(String(512), nullable=False)
	signed_contract: Mapped[Any] = mapped_column(String(128), nullable=False)
	bank_account_ref: Mapped[Any] = mapped_column(String(512), nullable=False)

	def __repr__(self) -> str:  # pragma: no cover - cosmetic
		return f"<CompleteHire id={self.id!r} state={self.state!r}>"
