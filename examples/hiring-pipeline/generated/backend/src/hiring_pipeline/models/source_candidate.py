"""SQLAlchemy model for Source a candidate — generated by flowforge JTBD generator.

Do not edit by hand; rerun ``flowforge new`` (or the generator unit
tests) to regenerate. Edits should happen in the JTBD bundle.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any

from sqlalchemy import (
	Boolean,
	Date,
	DateTime,
	Integer,
	Numeric,
	String,
	Text,
)
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
	"""Project-local declarative base; alembic upgrades target this metadata."""


class SourceCandidate(Base):
	__tablename__ = "source_candidate"

	id: Mapped[str] = mapped_column(String(36), primary_key=True)
	tenant_id: Mapped[str | None] = mapped_column(String(36), nullable=True, index=True)
	state: Mapped[str] = mapped_column(String(64), nullable=False, default="intake")
	created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
	candidate_name: Mapped[Any] = mapped_column(String(512), nullable=False)
	candidate_email: Mapped[Any] = mapped_column(String(320), nullable=False)
	candidate_phone: Mapped[Any] = mapped_column(String(40), nullable=True)
	role_title: Mapped[Any] = mapped_column(String(512), nullable=False)
	source_channel: Mapped[Any] = mapped_column(String(64), nullable=False)
	resume: Mapped[Any] = mapped_column(String(256), nullable=True)
	notes: Mapped[Any] = mapped_column(Text(), nullable=True)

	def __repr__(self) -> str:  # pragma: no cover - cosmetic
		return f"<SourceCandidate id={self.id!r} state={self.state!r}>"
