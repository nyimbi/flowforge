"""Typer CLI entry point for hiring-pipeline.

Generated by flowforge JTBD generator — do not edit by hand. Each JTBD
becomes a Typer subapp; each transition event becomes a command under
that subapp. The command tree mirrors the bundle's ``openapi.yaml``
(W1, item 8) one-to-one.

Run ``hiring-pipeline-cli --help`` to enumerate.
"""

from __future__ import annotations

import json
from typing import Any, Optional

import typer

from . import client


app = typer.Typer(
	help="Typer CLI client for hiring-pipeline — fires workflow events into the bundle's FastAPI backend.",
	no_args_is_help=True,
)

complete_hire_app = typer.Typer(
	help="Complete hire and onboard",
	no_args_is_help=True,
)
app.add_typer(complete_hire_app, name="complete-hire")
conduct_interview_app = typer.Typer(
	help="Conduct interview loop",
	no_args_is_help=True,
)
app.add_typer(conduct_interview_app, name="conduct-interview")
extend_offer_app = typer.Typer(
	help="Extend an offer",
	no_args_is_help=True,
)
app.add_typer(extend_offer_app, name="extend-offer")
screen_candidate_app = typer.Typer(
	help="Screen a candidate",
	no_args_is_help=True,
)
app.add_typer(screen_candidate_app, name="screen-candidate")
source_candidate_app = typer.Typer(
	help="Source a candidate",
	no_args_is_help=True,
)
app.add_typer(source_candidate_app, name="source-candidate")


@complete_hire_app.command("approve")
def complete_hire_approve(
	acceptance_date: str = typer.Option(..., help="Acceptance date (required)"),
	bank_account_ref: str = typer.Option(..., help="Payroll account ref (required)"),
	emergency_contact: str = typer.Option(..., help="Emergency contact (required)"),
	home_address: str = typer.Option(..., help="Home address (required)"),
	legal_name: str = typer.Option(..., help="Legal name (required)"),
	national_id_ref: str = typer.Option(..., help="National ID reference (required)"),
	signed_contract: str = typer.Option(..., help="Signed contract (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/complete-hire/events`` for Complete hire and onboard."""

	payload: dict[str, Any] = {}
	payload["acceptance_date"] = acceptance_date
	payload["bank_account_ref"] = bank_account_ref
	payload["emergency_contact"] = emergency_contact
	payload["home_address"] = home_address
	payload["legal_name"] = legal_name
	payload["national_id_ref"] = national_id_ref
	payload["signed_contract"] = signed_contract
	result = client.fire_sync(base_url, "complete-hire", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@complete_hire_app.command("reject")
def complete_hire_reject(
	acceptance_date: str = typer.Option(..., help="Acceptance date (required)"),
	bank_account_ref: str = typer.Option(..., help="Payroll account ref (required)"),
	emergency_contact: str = typer.Option(..., help="Emergency contact (required)"),
	home_address: str = typer.Option(..., help="Home address (required)"),
	legal_name: str = typer.Option(..., help="Legal name (required)"),
	national_id_ref: str = typer.Option(..., help="National ID reference (required)"),
	signed_contract: str = typer.Option(..., help="Signed contract (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``reject`` against ``/complete-hire/events`` for Complete hire and onboard."""

	payload: dict[str, Any] = {}
	payload["acceptance_date"] = acceptance_date
	payload["bank_account_ref"] = bank_account_ref
	payload["emergency_contact"] = emergency_contact
	payload["home_address"] = home_address
	payload["legal_name"] = legal_name
	payload["national_id_ref"] = national_id_ref
	payload["signed_contract"] = signed_contract
	result = client.fire_sync(base_url, "complete-hire", "reject", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@complete_hire_app.command("submit")
def complete_hire_submit(
	acceptance_date: str = typer.Option(..., help="Acceptance date (required)"),
	bank_account_ref: str = typer.Option(..., help="Payroll account ref (required)"),
	emergency_contact: str = typer.Option(..., help="Emergency contact (required)"),
	home_address: str = typer.Option(..., help="Home address (required)"),
	legal_name: str = typer.Option(..., help="Legal name (required)"),
	national_id_ref: str = typer.Option(..., help="National ID reference (required)"),
	signed_contract: str = typer.Option(..., help="Signed contract (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/complete-hire/events`` for Complete hire and onboard."""

	payload: dict[str, Any] = {}
	payload["acceptance_date"] = acceptance_date
	payload["bank_account_ref"] = bank_account_ref
	payload["emergency_contact"] = emergency_contact
	payload["home_address"] = home_address
	payload["legal_name"] = legal_name
	payload["national_id_ref"] = national_id_ref
	payload["signed_contract"] = signed_contract
	result = client.fire_sync(base_url, "complete-hire", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@conduct_interview_app.command("approve")
def conduct_interview_approve(
	culture_score: float = typer.Option(..., help="Culture score (1-5) (required)"),
	debrief_notes: str = typer.Option(..., help="Debrief notes (required)"),
	interview_date: str = typer.Option(..., help="Interview date/time (required)"),
	panel_scorecard: str = typer.Option(..., help="Panel scorecard summary (required)"),
	technical_score: float = typer.Option(..., help="Technical score (1-5) (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/conduct-interview/events`` for Conduct interview loop."""

	payload: dict[str, Any] = {}
	payload["culture_score"] = culture_score
	payload["debrief_notes"] = debrief_notes
	payload["interview_date"] = interview_date
	payload["panel_scorecard"] = panel_scorecard
	payload["technical_score"] = technical_score
	result = client.fire_sync(base_url, "conduct-interview", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@conduct_interview_app.command("submit")
def conduct_interview_submit(
	culture_score: float = typer.Option(..., help="Culture score (1-5) (required)"),
	debrief_notes: str = typer.Option(..., help="Debrief notes (required)"),
	interview_date: str = typer.Option(..., help="Interview date/time (required)"),
	panel_scorecard: str = typer.Option(..., help="Panel scorecard summary (required)"),
	technical_score: float = typer.Option(..., help="Technical score (1-5) (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/conduct-interview/events`` for Conduct interview loop."""

	payload: dict[str, Any] = {}
	payload["culture_score"] = culture_score
	payload["debrief_notes"] = debrief_notes
	payload["interview_date"] = interview_date
	payload["panel_scorecard"] = panel_scorecard
	payload["technical_score"] = technical_score
	result = client.fire_sync(base_url, "conduct-interview", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@extend_offer_app.command("approve")
def extend_offer_approve(
	base_salary: float = typer.Option(..., help="Base salary (required)"),
	bonus_target: Optional[float] = typer.Option(None, help="Bonus target"),
	equity_units: Optional[float] = typer.Option(None, help="RSU units"),
	offer_expiry: str = typer.Option(..., help="Offer expires (required)"),
	offer_letter: str = typer.Option(..., help="Offer letter PDF (required)"),
	start_date: str = typer.Option(..., help="Proposed start (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/extend-offer/events`` for Extend an offer."""

	payload: dict[str, Any] = {}
	payload["base_salary"] = base_salary
	if bonus_target is not None:
		payload["bonus_target"] = bonus_target
	if equity_units is not None:
		payload["equity_units"] = equity_units
	payload["offer_expiry"] = offer_expiry
	payload["offer_letter"] = offer_letter
	payload["start_date"] = start_date
	result = client.fire_sync(base_url, "extend-offer", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@extend_offer_app.command("escalate")
def extend_offer_escalate(
	base_salary: float = typer.Option(..., help="Base salary (required)"),
	bonus_target: Optional[float] = typer.Option(None, help="Bonus target"),
	equity_units: Optional[float] = typer.Option(None, help="RSU units"),
	offer_expiry: str = typer.Option(..., help="Offer expires (required)"),
	offer_letter: str = typer.Option(..., help="Offer letter PDF (required)"),
	start_date: str = typer.Option(..., help="Proposed start (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``escalate`` against ``/extend-offer/events`` for Extend an offer."""

	payload: dict[str, Any] = {}
	payload["base_salary"] = base_salary
	if bonus_target is not None:
		payload["bonus_target"] = bonus_target
	if equity_units is not None:
		payload["equity_units"] = equity_units
	payload["offer_expiry"] = offer_expiry
	payload["offer_letter"] = offer_letter
	payload["start_date"] = start_date
	result = client.fire_sync(base_url, "extend-offer", "escalate", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@extend_offer_app.command("reject")
def extend_offer_reject(
	base_salary: float = typer.Option(..., help="Base salary (required)"),
	bonus_target: Optional[float] = typer.Option(None, help="Bonus target"),
	equity_units: Optional[float] = typer.Option(None, help="RSU units"),
	offer_expiry: str = typer.Option(..., help="Offer expires (required)"),
	offer_letter: str = typer.Option(..., help="Offer letter PDF (required)"),
	start_date: str = typer.Option(..., help="Proposed start (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``reject`` against ``/extend-offer/events`` for Extend an offer."""

	payload: dict[str, Any] = {}
	payload["base_salary"] = base_salary
	if bonus_target is not None:
		payload["bonus_target"] = bonus_target
	if equity_units is not None:
		payload["equity_units"] = equity_units
	payload["offer_expiry"] = offer_expiry
	payload["offer_letter"] = offer_letter
	payload["start_date"] = start_date
	result = client.fire_sync(base_url, "extend-offer", "reject", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@extend_offer_app.command("submit")
def extend_offer_submit(
	base_salary: float = typer.Option(..., help="Base salary (required)"),
	bonus_target: Optional[float] = typer.Option(None, help="Bonus target"),
	equity_units: Optional[float] = typer.Option(None, help="RSU units"),
	offer_expiry: str = typer.Option(..., help="Offer expires (required)"),
	offer_letter: str = typer.Option(..., help="Offer letter PDF (required)"),
	start_date: str = typer.Option(..., help="Proposed start (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/extend-offer/events`` for Extend an offer."""

	payload: dict[str, Any] = {}
	payload["base_salary"] = base_salary
	if bonus_target is not None:
		payload["bonus_target"] = bonus_target
	if equity_units is not None:
		payload["equity_units"] = equity_units
	payload["offer_expiry"] = offer_expiry
	payload["offer_letter"] = offer_letter
	payload["start_date"] = start_date
	result = client.fire_sync(base_url, "extend-offer", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@screen_candidate_app.command("approve")
def screen_candidate_approve(
	availability: Optional[str] = typer.Option(None, help="Available from"),
	salary_expect: Optional[float] = typer.Option(None, help="Salary expectation"),
	screen_notes: str = typer.Option(..., help="Screen notes (required)"),
	screen_score: float = typer.Option(..., help="Screen score (1-5) (required)"),
	years_exp: float = typer.Option(..., help="Years experience (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/screen-candidate/events`` for Screen a candidate."""

	payload: dict[str, Any] = {}
	if availability is not None:
		payload["availability"] = availability
	if salary_expect is not None:
		payload["salary_expect"] = salary_expect
	payload["screen_notes"] = screen_notes
	payload["screen_score"] = screen_score
	payload["years_exp"] = years_exp
	result = client.fire_sync(base_url, "screen-candidate", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@screen_candidate_app.command("submit")
def screen_candidate_submit(
	availability: Optional[str] = typer.Option(None, help="Available from"),
	salary_expect: Optional[float] = typer.Option(None, help="Salary expectation"),
	screen_notes: str = typer.Option(..., help="Screen notes (required)"),
	screen_score: float = typer.Option(..., help="Screen score (1-5) (required)"),
	years_exp: float = typer.Option(..., help="Years experience (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/screen-candidate/events`` for Screen a candidate."""

	payload: dict[str, Any] = {}
	if availability is not None:
		payload["availability"] = availability
	if salary_expect is not None:
		payload["salary_expect"] = salary_expect
	payload["screen_notes"] = screen_notes
	payload["screen_score"] = screen_score
	payload["years_exp"] = years_exp
	result = client.fire_sync(base_url, "screen-candidate", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@source_candidate_app.command("approve")
def source_candidate_approve(
	candidate_email: str = typer.Option(..., help="Email (required)"),
	candidate_name: str = typer.Option(..., help="Full name (required)"),
	candidate_phone: Optional[str] = typer.Option(None, help="Phone"),
	notes: Optional[str] = typer.Option(None, help="Notes"),
	resume: Optional[str] = typer.Option(None, help="Resume"),
	role_title: str = typer.Option(..., help="Role applied (required)"),
	source_channel: str = typer.Option(..., help="Source channel (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``approve`` against ``/source-candidate/events`` for Source a candidate."""

	payload: dict[str, Any] = {}
	payload["candidate_email"] = candidate_email
	payload["candidate_name"] = candidate_name
	if candidate_phone is not None:
		payload["candidate_phone"] = candidate_phone
	if notes is not None:
		payload["notes"] = notes
	if resume is not None:
		payload["resume"] = resume
	payload["role_title"] = role_title
	payload["source_channel"] = source_channel
	result = client.fire_sync(base_url, "source-candidate", "approve", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@source_candidate_app.command("reject")
def source_candidate_reject(
	candidate_email: str = typer.Option(..., help="Email (required)"),
	candidate_name: str = typer.Option(..., help="Full name (required)"),
	candidate_phone: Optional[str] = typer.Option(None, help="Phone"),
	notes: Optional[str] = typer.Option(None, help="Notes"),
	resume: Optional[str] = typer.Option(None, help="Resume"),
	role_title: str = typer.Option(..., help="Role applied (required)"),
	source_channel: str = typer.Option(..., help="Source channel (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``reject`` against ``/source-candidate/events`` for Source a candidate."""

	payload: dict[str, Any] = {}
	payload["candidate_email"] = candidate_email
	payload["candidate_name"] = candidate_name
	if candidate_phone is not None:
		payload["candidate_phone"] = candidate_phone
	if notes is not None:
		payload["notes"] = notes
	if resume is not None:
		payload["resume"] = resume
	payload["role_title"] = role_title
	payload["source_channel"] = source_channel
	result = client.fire_sync(base_url, "source-candidate", "reject", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


@source_candidate_app.command("submit")
def source_candidate_submit(
	candidate_email: str = typer.Option(..., help="Email (required)"),
	candidate_name: str = typer.Option(..., help="Full name (required)"),
	candidate_phone: Optional[str] = typer.Option(None, help="Phone"),
	notes: Optional[str] = typer.Option(None, help="Notes"),
	resume: Optional[str] = typer.Option(None, help="Resume"),
	role_title: str = typer.Option(..., help="Role applied (required)"),
	source_channel: str = typer.Option(..., help="Source channel (required)"),
	base_url: str = typer.Option("http://localhost:8000", "--base-url", help="FastAPI backend base URL."),
) -> None:
	"""Fire ``submit`` against ``/source-candidate/events`` for Source a candidate."""

	payload: dict[str, Any] = {}
	payload["candidate_email"] = candidate_email
	payload["candidate_name"] = candidate_name
	if candidate_phone is not None:
		payload["candidate_phone"] = candidate_phone
	if notes is not None:
		payload["notes"] = notes
	if resume is not None:
		payload["resume"] = resume
	payload["role_title"] = role_title
	payload["source_channel"] = source_channel
	result = client.fire_sync(base_url, "source-candidate", "submit", payload)
	typer.echo(json.dumps(result, indent=2, sort_keys=True))


if __name__ == "__main__":
	app()
