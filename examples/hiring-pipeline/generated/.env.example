# hiring-pipeline — generated by flowforge JTBD generator
# Copy to .env and fill in real values before running.

DATABASE_URL=sqlite+pysqlite:///hiring_pipeline.db
TENANCY=multi
DEFAULT_LOCALE=en
DEFAULT_CURRENCY=USD

# Workflow definitions (JSON DSL) live alongside the backend.
FLOWFORGE_WORKFLOWS_DIR=workflows
