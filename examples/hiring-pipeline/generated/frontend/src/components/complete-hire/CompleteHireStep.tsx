// Complete hire and onboard step component — generated by flowforge JTBD generator.
// Re-run the generator to regenerate; do not edit by hand.
import * as React from "react";

export interface CompleteHireStepProps {
	instanceId: string;
	state: string;
	onEvent: (event: string, payload?: Record<string, unknown>) => Promise<void> | void;
}

const FIELDS: ReadonlyArray<{ id: string; label: string; required: boolean; pii: boolean }> = [
	{ id: "acceptance_date", label: "Acceptance date", required: true, pii: false },
	{ id: "legal_name", label: "Legal name", required: true, pii: true },
	{ id: "national_id_ref", label: "National ID reference", required: true, pii: true },
	{ id: "home_address", label: "Home address", required: true, pii: true },
	{ id: "emergency_contact", label: "Emergency contact", required: true, pii: true },
	{ id: "signed_contract", label: "Signed contract", required: true, pii: true },
	{ id: "bank_account_ref", label: "Payroll account ref", required: true, pii: true },
];

/**
 * Minimal review step for the complete_hire workflow. Hosts wrap this with
 * the project-wide auth/tenant context — keeping the component dumb so
 * generated code stays portable across Next.js and plain Vite apps.
 */
export function CompleteHireStep(props: CompleteHireStepProps): React.ReactElement {
	const [busy, setBusy] = React.useState(false);

	const fire = React.useCallback(
		async (event: string) => {
			if (busy) return;
			setBusy(true);
			try {
				await props.onEvent(event, {});
			} finally {
				setBusy(false);
			}
		},
		[busy, props],
	);

	return (
		<section data-testid="complete_hire-step" aria-labelledby="complete_hire-step-title">
			<header>
				<h2 id="complete_hire-step-title">Complete hire and onboard</h2>
				<p>State: <code>{props.state}</code></p>
				<p>Instance: <code>{props.instanceId}</code></p>
			</header>
			<dl>
				{FIELDS.map((field) => (
					<React.Fragment key={field.id}>
						<dt>{field.label}{field.required ? " *" : ""}{field.pii ? " (PII)" : ""}</dt>
						<dd>—</dd>
					</React.Fragment>
				))}
			</dl>
			<footer>
				<button type="button" disabled={busy} onClick={() => fire("submit")}>
					Submit
				</button>
				<button type="button" disabled={busy} onClick={() => fire("approve")}>
					Approve
				</button>
			</footer>
		</section>
	);
}
