// Conduct interview loop step component — generated by flowforge JTBD generator.
// Re-run the generator to regenerate; do not edit by hand.
import * as React from "react";

export interface ConductInterviewStepProps {
	instanceId: string;
	state: string;
	onEvent: (event: string, payload?: Record<string, unknown>) => Promise<void> | void;
}

const FIELDS: ReadonlyArray<{ id: string; label: string; required: boolean; pii: boolean }> = [
	{ id: "panel_scorecard", label: "Panel scorecard summary", required: true, pii: false },
	{ id: "technical_score", label: "Technical score (1-5)", required: true, pii: false },
	{ id: "culture_score", label: "Culture score (1-5)", required: true, pii: false },
	{ id: "debrief_notes", label: "Debrief notes", required: true, pii: false },
	{ id: "interview_date", label: "Interview date/time", required: true, pii: false },
];

/**
 * Minimal review step for the conduct_interview workflow. Hosts wrap this with
 * the project-wide auth/tenant context — keeping the component dumb so
 * generated code stays portable across Next.js and plain Vite apps.
 */
export function ConductInterviewStep(props: ConductInterviewStepProps): React.ReactElement {
	const [busy, setBusy] = React.useState(false);

	const fire = React.useCallback(
		async (event: string) => {
			if (busy) return;
			setBusy(true);
			try {
				await props.onEvent(event, {});
			} finally {
				setBusy(false);
			}
		},
		[busy, props],
	);

	return (
		<section data-testid="conduct_interview-step" aria-labelledby="conduct_interview-step-title">
			<header>
				<h2 id="conduct_interview-step-title">Conduct interview loop</h2>
				<p>State: <code>{props.state}</code></p>
				<p>Instance: <code>{props.instanceId}</code></p>
			</header>
			<dl>
				{FIELDS.map((field) => (
					<React.Fragment key={field.id}>
						<dt>{field.label}{field.required ? " *" : ""}{field.pii ? " (PII)" : ""}</dt>
						<dd>—</dd>
					</React.Fragment>
				))}
			</dl>
			<footer>
				<button type="button" disabled={busy} onClick={() => fire("submit")}>
					Submit
				</button>
				<button type="button" disabled={busy} onClick={() => fire("approve")}>
					Approve
				</button>
			</footer>
		</section>
	);
}
