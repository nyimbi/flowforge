// Next.js app-router page for the Extend an offer workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { ExtendOfferStep } from "@/components/extend-offer/ExtendOfferStep";

export const dynamic = "force-dynamic";

export default function ExtendOfferPage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/extend-offer/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Extend an offer</h1>
			<ExtendOfferStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
