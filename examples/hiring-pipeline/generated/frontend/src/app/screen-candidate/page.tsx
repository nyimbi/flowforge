// Next.js app-router page for the Screen a candidate workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { ScreenCandidateStep } from "@/components/screen-candidate/ScreenCandidateStep";

export const dynamic = "force-dynamic";

export default function ScreenCandidatePage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/screen-candidate/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Screen a candidate</h1>
			<ScreenCandidateStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
