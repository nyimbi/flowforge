// Next.js app-router page for the Conduct interview loop workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { ConductInterviewStep } from "@/components/conduct-interview/ConductInterviewStep";

export const dynamic = "force-dynamic";

export default function ConductInterviewPage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/conduct-interview/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Conduct interview loop</h1>
			<ConductInterviewStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
