// Next.js app-router page for the Source a candidate workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { SourceCandidateStep } from "@/components/source-candidate/SourceCandidateStep";

export const dynamic = "force-dynamic";

export default function SourceCandidatePage(): React.ReactElement {
	const [state, setState] = React.useState("intake");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/source-candidate/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>Source a candidate</h1>
			<SourceCandidateStep instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
