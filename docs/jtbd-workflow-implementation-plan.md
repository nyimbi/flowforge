# JTBD Workflow Implementation Plan

Date started: 2026-04-30

Source catalogue: `docs/platform-jtbd-catalogue.md`

This file is the continuation ledger for the Ralph implementation loop. Update it after every completed slice.

## Status Legend

| Status | Meaning |
|---|---|
| Not started | No implementation work in this Ralph program. |
| In progress | Slice is actively being implemented. |
| Blocked | External dependency or decision required. |
| Verified | Implementation and targeted verification complete. |
| Committed | Verified slice has been committed. |
| Pushed | Commit has been pushed to remote. |

## Slice Tracker

| Slice | Priority | JTBD IDs | Current support | Backend changes required | Frontend changes required | Data/migration changes | Workflow/task/notification/audit changes | Tests required | Status | Verification | Commit | Push |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| S01 Baseline and ownership audit | P0 | all | Dirty worktree contains prior workflow edits; exact ownership unknown. | Inspect existing changes, identify reusable completed work, avoid reverting unrelated edits. | Inspect related workflow UI changes. | None expected. | Establish tracker and state. | `git status`, targeted diff inspection, no code tests required. | Pushed | `git status --short`, `git diff --stat`, targeted workflow diff inspection, `git diff --check` passed. | `9e9fe33` | Pushed to `origin/jtbd-workflow-support`. |
| S02 Normalize application-to-policy lifecycle | P0 | JTBD-012 to JTBD-050, JTBD-064 to JTBD-066 | Partial; application workflow exists but compresses richer lifecycle. | Added `policy.execution`, start-on-issue, policy subject actions, and workflow engine/read-side hardening. | Policy detail now displays `policy.execution` and prefers live instance state. | No migration added in this slice; ORM constraint hardening requires migration follow-up before deployment. | Workflow definitions, graph metadata, next-actions, pause/resume reason support, and policy task subject coverage. | Backend workflow/action tests; frontend state/action tests. | Pushed | `uv run pytest tests/test_policy_execution_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_audit_workflows_reads.py tests/test_workflows_pause_resume.py tests/test_operational_tasks.py -q`; `uv run pytest tests/test_pr15_jit_writes.py::test_issue_policy_calls_write_resource_grants tests/test_pr15_otel_spans.py::test_policies_issue_policy_triggers_span -q`; `uv run ruff check app/workflows/definitions/policy_execution.py tests/test_policy_execution_workflow.py`; `npm run test -- src/components/workflows/__tests__`; `npm run typecheck`; `git diff --check` all passed. Broader touched-file ruff still reports pre-existing style debt in dirty files. | `780461b` | Pushed to `origin/jtbd-workflow-support`. |
| S03 Expand claim determination workflow | P0 | JTBD-073 to JTBD-083 | Partial; claim workflow exists but is coarser than PRD. | Added `determination_pending`, gated final adjudication on final loss, indemnity, and approved `claim.adjudicate` approval. | Claim detail can execute backend workflow actions and shows determination/payment-readiness state. | No migration added in this slice; determination metadata uses existing claim fields and workflow events. | Claim workflow transitions, available-action labels, action execution, query invalidation, and gate errors. | Claim workflow happy/blocked/authority/audit/task tests. | Pushed | `uv run pytest tests/test_claim_determination_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/workflows/definitions/claim_trade_credit.py tests/test_claim_determination_workflow.py`; `npm run test -- src/lib/__tests__/claim-workflow-api.test.ts src/components/workflows/__tests__/subject-workflow-section.test.tsx src/components/workflows/__tests__/next-action-panel.test.tsx`; `npm run typecheck`; `git diff --check` all passed. | `759572d` | Pushed to `origin/jtbd-workflow-support`. |
| S04 Reusable missing-information workflow | P0 | JTBD-033, JTBD-075, JTBD-110 to JTBD-112 | Partial; checklists/docs existed but MIR was not first-class. | Added reusable `information.request` workflow, service/router, subject-scoped list/create/resolve APIs, task projection, and workflow available-actions. | Added reusable missing-information panel/client and mounted it on application detail. | No new table in this slice; MIRs use workflow instances plus operational-task attrs. | Tasks, due dates, communication state, workflow actions, and audit-ready transition payloads. Notifications still use existing workflow notify hooks. | Backend MIR tests; frontend MIR interaction tests. | Pushed | `uv run pytest tests/test_information_requests.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/information_requests app/workflows/definitions/information_request.py tests/test_information_requests.py`; `npm run test -- missing-information`; `npm run typecheck`; `git diff --check` all passed. | `76caca6` | Pushed to `origin/jtbd-workflow-support`. |
| S05 First-class reinsurance concurrence | P0 | JTBD-059, JTBD-080, JTBD-113 | Partial; reinsurance rules and fac/slip surfaces existed, but concurrence was not a first-class workflow object. | Added `reinsurance.concurrence` workflow, subject-scoped concurrence request API/service, task projection, response actions, and claim adjudication gate. | Added reusable concurrence panel/client; mounted it in reinsurance workspace and claim detail. | No new table in this slice; concurrence requests use workflow instances plus operational-task attrs. | Tasks, SLA due dates, `awaiting_reinsurer` communication state, workflow events, and claim-gate blocking for required concurrence. | Concurrence workflow/gate tests; UI tests. | Pushed | `uv run pytest tests/test_reinsurance_concurrence_workflow.py tests/test_claim_determination_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/reinsurance/concurrence.py app/reinsurance/views.py app/workflows/definitions/reinsurance_concurrence.py tests/test_reinsurance_concurrence_workflow.py tests/test_claim_determination_workflow.py app/workflows/definitions/claim_trade_credit.py --select E,F,I,UP,B,SIM,TCH,RUF`; `uv run ruff check app/reinsurance/router.py --select F,I`; `npm run test -- reinsurance-concurrence concurrence-panel reinsurance-workspace`; `npm run typecheck`; `git diff --check` all passed. | `f360d52` | Pushed to `origin/jtbd-workflow-support`. |
| S06 High-risk admin change workflow | P0 | JTBD-099 to JTBD-104 | Partial; admin surfaces existed but lifecycle was fragmented. | Added `admin.change` workflow, governed admin-change API/service, task projection, dry-run/review/approve/publish/rollback actions, and available-actions support. | Added admin change queue/client and mounted it on the settings registry surface. | No new table in this slice; admin changes use workflow instances plus operational-task attrs. | Tasks, SLA due dates, `awaiting_admin_review` communication state, workflow events, and rollback-state handling. | Admin change workflow/API tests; frontend admin queue/API tests. | Pushed | `uv run pytest tests/test_admin_change_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/admin/change_workflow.py app/admin/change_workflow_router.py app/admin/change_workflow_views.py app/workflows/definitions/admin_change.py tests/test_admin_change_workflow.py`; `uv run ruff check app/api/router.py app/tasks/models.py app/tasks/service.py --select F,I`; `npm run test -- admin-change AdminChangeQueue`; `npm run typecheck`; `git diff --check` all passed. | `302c802` | Pushed to `origin/jtbd-workflow-support`. |
| S07 Finance exception workflow | P0 | JTBD-091 to JTBD-094 | Partial/live per domain but not unified. | Added `finance.exception` workflow, finance exception API/service, task projection, review/approve/reject/resolve actions, and available-actions support. | Added finance exception queue/client and mounted it on the admin finance surface. | No new table in this slice; finance exceptions use workflow instances plus operational-task attrs. | Tasks, SLA due dates, `awaiting_admin_review` communication state, workflow events, and resolution-state handling. | Finance workflow/API tests; frontend finance queue/API tests. | Pushed | `uv run pytest tests/test_finance_exception_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/finance_exceptions app/workflows/definitions/finance_exception.py app/workflows/definitions/__init__.py app/api/router.py tests/test_finance_exception_workflow.py`; `uv run ruff check app/tasks/models.py app/tasks/service.py --select F,I`; `npm run test -- finance-exception FinanceExceptionQueue`; `npm run typecheck`; `git diff --check` all passed. | `0e3910a` | Pushed to `origin/jtbd-workflow-support`. |
| S08 Broker acceptance workflow | P1 | JTBD-016 to JTBD-018 | Partial; party KYC existed but broker panel status was not workflow-controlled. | Added `broker.acceptance` workflow, broker acceptance API/service, task projection, party status/attrs updates, renewal/expiry checks, and binding guard blocker. | Added broker acceptance panel/client and mounted it on party detail. | No new table in this slice; acceptance cases use workflow instances plus operational-task attrs and party attrs. | Tasks, due dates, `awaiting_broker` communication state, workflow events, and binding-block evidence. | Broker acceptance workflow/gate tests; frontend broker acceptance tests. | Pushed | `uv run pytest tests/test_broker_acceptance_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py tests/test_binding_guard.py -q`; `uv run ruff check app/brokers/acceptance.py app/workflows/definitions/broker_acceptance.py tests/test_broker_acceptance_workflow.py`; `uv run ruff check app/brokers/router.py app/brokers/views.py app/workflows/definitions/__init__.py app/tasks/models.py app/tasks/service.py app/underwriting/binding.py app/underwriting/referral_engine.py --select F,I`; `npm run test -- broker-acceptance BrokerAcceptancePanel`; `npm run typecheck`; `git diff --check` all passed. | `99bb649` | Pushed to `origin/jtbd-workflow-support`. |
| S09 Specialist review workflows | P1 | JTBD-041 to JTBD-045 | Partial; specialist analysis was not a reusable workflow/task subject. | Added `specialist.review` workflow, specialist review API/service, task projection, list/action support, and available-actions support. | Added reusable specialist review panel/client and mounted it on application detail with create/action flows. | No new table in this slice; specialist reviews use workflow instances plus operational-task attrs. | Tasks, SLA due dates, `awaiting_underwriter` communication state, workflow events, recommendation capture, and terminal task completion. | Specialist review workflow/API tests; frontend specialist review tests. | Pushed | `uv run pytest tests/test_specialist_review_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/specialist_reviews app/workflows/definitions/specialist_review.py tests/test_specialist_review_workflow.py`; `uv run ruff check app/api/router.py app/workflows/definitions/__init__.py app/tasks/models.py app/tasks/service.py tests/test_operational_tasks.py --select F,I`; `npm run test -- specialist-review SpecialistReviewPanel`; `git diff --check` all passed. `npm run typecheck` is currently blocked by unrelated dirty `frontend/src/components/rich-document-view.tsx` TS7022. | `54335c0` | Pushed to `origin/jtbd-workflow-support`. |
| S10 Policy obligation lifecycle | P1 | JTBD-069 | Live/partial; persisted diary existed but was not workflow/task-backed and used skip/overdue semantics. | Added `policy.obligation` workflow, obligation task projection, explicit waive/cancel/breach semantics, evidence-required satisfaction, and available-actions support. | Extended obligation diary UI with evidence-backed satisfy, waive, and escalate controls. | No migration needed; existing `policy_obligations` table stores new statuses without DB enum constraints. | Materialization starts workflow/task rows; overdue recompute marks obligations breached and raises urgent operational tasks; terminal actions complete tasks and audit remains recorded. | Obligation workflow/state-machine/task tests; frontend obligation diary tests. | Pushed | `uv run pytest tests/test_policy_obligation_diary.py tests/test_policy_obligations.py tests/test_policy_execution_workflow.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/policies/obligations.py app/workflows/definitions/policy_obligation.py tests/test_policy_obligation_diary.py --select E,F,I,UP,B,SIM,TCH,RUF`; `uv run ruff check app/policies/router.py app/policies/views.py app/workflows/definitions/__init__.py app/tasks/models.py app/tasks/service.py app/tasks/router.py tests/test_workflow_definitions.py tests/test_operational_tasks.py --select F,I`; `npm run test -- ObligationDiaryPanel`; `npm run typecheck`; `git diff --check` all passed. | `15eb221` | Pushed to `origin/main`. |
| S11 Migration exception workflow | P1 | JTBD-108 to JTBD-109 | Live/partial. | Added `migration.exception` workflow, exception list/action APIs, task projection, match-time exception projection, and terminal disposition handling for corrected/accepted/rejected/archived imports. | Added batch-review exception remediation controls for review, accept-with-warning, and archive actions. | No schema migration needed; existing `migration_items` columns store exception and disposition state. | Exceptions now start workflow/task rows, surface available-actions, complete tasks on terminal disposition, and keep audit-compatible decision notes. | Migration workflow/service tests; operational-task action tests; frontend batch exception action test. | Pushed | `uv run pytest tests/test_migration_exception_workflow.py tests/test_migration_workbench.py tests/test_workflow_definitions.py tests/test_workflow_engine.py tests/test_operational_tasks.py -q`; `uv run ruff check app/migration/router.py app/migration/service.py app/migration/views.py app/workflows/definitions/migration_exception.py tests/test_migration_exception_workflow.py tests/test_workflow_definitions.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `npm run test -- batch-detail`; `npm run typecheck` all passed. | `bb171c7` | Pushed to `origin/main`. |
| S12 Notification quick actions | P1 | JTBD-010, JTBD-116 | Partial; notification lifecycle existed but rows did not expose action metadata to the inbox/bell. | Added typed notification quick-action payloads, persisted/broadcast quick actions, and inbox enrichment from subject `available-actions`. | Inbox and bell now render quick-action buttons/links, execute explicit action API paths, acknowledge handled notifications, and invalidate notification/task/action queries. | No schema migration needed; quick actions live in validated notification attrs and computed read DTOs. | Notifications now carry normalized action metadata while domain APIs remain authoritative for permission/gate enforcement. | Notification payload/enrichment tests; frontend inbox and bell quick-action tests. | Pushed | `uv run pytest tests/test_pr15_notifications.py tests/test_api_routes_registered.py -q`; `uv run ruff check app/notifications/router.py app/notifications/service.py app/notifications/views.py tests/test_pr15_notifications.py --select E,F,I,B,SIM`; `npm run test -- notifications-page app-shell`; `npm run typecheck`; `git diff --check` all passed. | `df8dc95` | Pushed to `origin/main`. |
| S13 Command palette actions | P2 | JTBD-011 | Live search and static command palette existed, but palette actions were only fixed navigation shortcuts. | No backend change needed; palette reuses search and subject `available-actions`. | Added command search results, subject next-action commands, and a shared command-action executor for supported no-reason actions with deep-link fallback for unsupported/reason-required actions. | None. | Uses the existing `available-actions` contract and invalidates action/task/subject query families after execution. | Command palette and command-action executor tests. | Pushed | `npm run test -- command-palette command-action-api`; `npm run test -- notifications-page app-shell`; `npm run typecheck`; `git diff --check` all passed. | `17b7a62` | Pushed to `origin/main`. |
| S14 AI provenance | P2 | JTBD-097 to JTBD-098 | Partial/live; assist results had confidence/citations, but no first-class provenance object, persisted suggestion record, or shared UI metadata block. | Added `AiProvenance`, persisted provenance on AI usage evidence and surfaced suggestion rows, and created a provenance migration for `ai_suggestions`. | Added shared provenance rendering across AI side panels, drawers, chips, inbox cards, coverage triage, command assistant slash results, and document classification chips. | `20260430_0056_ai_suggestion_provenance.py` adds `ai_suggestions.provenance`. | Preview now normalizes model/provider/confidence/cache/source-count/input-hash/invocation metadata, records a suggestion id usable for feedback, and stores provenance in audit-compatible evidence. | AI framework/router/service tests; frontend AI provenance surface tests. | Pushed | `uv run pytest tests/test_ai_assist_framework.py tests/test_ai_assist_router.py tests/test_ai_assist_services.py -q`; `uv run ruff check app/ai_assist/contracts.py app/ai_assist/ai_models.py app/ai_assist/assist_router.py tests/test_ai_assist_framework.py tests/test_ai_assist_router.py --select E,F,I,B,SIM`; `npm run test -- AssistChip CmdKAssistant ai-assist-types ai-assist-api AssistSidePanel AssistDrawer AssistInboxCard CoverageTriageBanner`; `npm run typecheck`; `git diff --check` all passed. | `28a8689` | Pushed to `origin/main`. |
| S15 Story-mode audit per lifecycle | P2 | JTBD-105 | Live; story mode now merges audit and workflow lifecycle evidence. | Added registry-derived workflow transition metadata to story events and grouped chapters by lifecycle definition/state path. | Audit story renders workflow definition chips, state paths, transition summaries, and is mounted on entity timeline tabs. | None. | Workflow definition, transition SLA/assignee/terminal-state metadata now flows into audit story payloads. | Audit story unit/API tests and frontend story rendering tests. | Pushed | `uv run pytest tests/test_audit_story.py tests/test_audit_timeline.py -q`; `uv run ruff check backend/app/audit/router.py backend/app/audit/story.py backend/app/audit/views.py backend/tests/test_audit_story.py backend/tests/test_audit_timeline.py --select E,F,I,B,SIM`; `npm run test -- audit-story entity-timeline`; `npm run typecheck`; `git diff --check` on S15 paths all passed. | `05fc4cf` | Pushed to `origin/main`. |
| S15R Actionability remediation | P0 | JTBD-010 to JTBD-011, JTBD-105, JTBD-116 | Audit found computed quick actions opened records, command fallback links missed workflow-backed subjects, and recovery timelines rejected `recovery_case`. | Added executable notification quick-action metadata for safe no-reason subject actions and allowed recovery-case timelines. | Added command palette routes for workflow-backed subject kinds and direct application submit/bind execution. | None. | Notification and command actions now use domain APIs when safe; reason-required actions still deep-link for audit reason capture. | Notification enrichment, timeline, command action, command palette, and app shell tests. | Pushed | `uv run pytest tests/test_audit_timeline.py tests/test_pr15_notifications.py -q`; `uv run ruff check app/audit/router.py app/notifications/router.py tests/test_audit_timeline.py tests/test_pr15_notifications.py --select E,F,I,B,SIM`; `npm run test -- command-action-api command-palette app-shell`; `npm run typecheck`; `git diff --check` all passed. | `f7d25d0` | Pushed to `origin/main`. |
| S16A Operator handover brief | P0 | JTBD-009 | Planned; no first-class handover artifact existed. | Added `GET /api/tasks/handover` generated from live OperationalTask inbox truth, grouped by owner, unclaimed, SLA breached, blocked, awaiting external, and critical/escalated work. | Added operations desk handover panel and task API parsing. | None. | Handover remains a read model over task/workflow projections; no new workflow table. | Handover grouping, route mount, task API, and tasks page tests. | Pushed | `uv run pytest tests/test_operational_tasks.py tests/test_api_routes_registered.py -q`; `uv run ruff check app/tasks/router.py app/tasks/views.py tests/test_operational_tasks.py tests/test_api_routes_registered.py --select E,F,I,B,SIM`; `npm run test -- tasks-page tasks-bulk`; `npm run typecheck`; `git diff --check` all passed. | `94c245a` | Pushed to `origin/main`. |
| S16B Operational SLA auto-escalation | P0 | JTBD-003, JTBD-005, JTBD-008 | SLA scanner projected breach investigation tasks, but the overdue source task stayed at its previous priority/severity and lacked escalation ledger evidence. | Added reusable task-domain SLA breach marking that promotes overdue source tasks to urgent/critical, stamps breach metadata, and records an automatic escalation attempt. | Existing task queues and handover surfaces now receive urgent/critical source tasks through the task DTO. | None. | SLA scanner remains idempotent through `breach_flagged_at` while projecting child breach investigation work. | SLA scanner/task attempt regression tests. | Pushed | `uv run pytest tests/test_operational_tasks.py tests/test_api_routes_registered.py -q`; `uv run ruff check app/tasks/service.py app/workers/tasks.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `git diff --check` all passed. | `be6337e` | Pushed to `origin/main`. |
| S16C Escalation evidence contract | P0 | JTBD-001, JTBD-003 to JTBD-008 | Auto-escalated SLA tasks carried evidence in attrs, but task DTOs and executive/operations views still inferred escalation from incomplete fields. | Added normalized `breach_flagged_at`, `escalated_at`, `escalated_to`, and `escalation_reason` task DTO evidence and aligned manual/automatic escalation attrs. | Operations saved views and cockpit exception board now use shared SLA/escalation predicates and safe title fallbacks. | None. | Handover and dashboards now consume the same breach/escalation evidence contract as task mutation/scanner paths. | Task DTO, handover, operations page, cockpit dashboard, and task API parsing tests. | Pushed | `uv run pytest tests/test_operational_tasks.py tests/test_api_routes_registered.py -q`; `uv run ruff check app/tasks/service.py app/tasks/router.py app/tasks/views.py app/workers/tasks.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `npm run test -- tasks-page cockpit-dashboard tasks-bulk`; `npm run typecheck`; `git diff --check` all passed. | `87c1936` | Pushed to `origin/main`. |
| S16D Workflow pause-clock SLA evidence | P0 | JTBD-003, JTBD-005, JTBD-008 | Paused workflow instances blocked transitions and SLA scans, but resume did not preserve elapsed paused time or extend active deadlines. | Added pause-clock context history and resume-time `due_at` extension by elapsed pause duration. | Existing workflow surfaces receive adjusted deadline/read evidence through workflow instance DTOs. | None. | Pause/resume events remain append-only; context now carries total paused seconds and last interval evidence. | Pause/resume, paused-action, audit-read regression tests. | Pushed | `uv run pytest tests/test_workflows_pause_resume.py tests/test_w8i_pause.py tests/test_audit_workflows_reads.py -q`; `uv run ruff check app/workflows/service.py tests/test_workflows_pause_resume.py --select E,F,I,B,SIM`; `git diff --check` all passed. | `9932f76` | Pushed to `origin/main`. |
| S16E Declarative workflow notifications | P0 | JTBD-001, JTBD-003 to JTBD-008 | `notify_on_enter` metadata was declared and exported, but transitions did not execute it; SLA breach notifications only used assignee fallback. | Added workflow notification recipient resolution and transition-time `notify_on_enter` materialization; SLA breach dispatch now reuses the same role/context/assignee resolver. | Existing notification inbox and quick-action surfaces receive workflow transition and SLA breach notices. | None. | Workflow transition notifications and breach notifications now use declarative workflow metadata with role evidence in attrs. | Workflow engine notification and SLA breach notification regression tests. | Pushed | `uv run pytest tests/test_workflow_engine.py tests/test_pr15_notifications.py tests/test_workflows_pause_resume.py tests/test_operational_tasks.py -q`; `uv run ruff check app/workflows/engine.py app/workflows/notifications.py app/workers/tasks.py tests/test_workflow_engine.py tests/test_pr15_notifications.py --select E,F,I,B,SIM`; `git diff --check` all passed. | `469be3c` | Pushed to `origin/main`. |
| S16 Executive/operations SLA escalation | P0 | JTBD-001, JTBD-003 to JTBD-009 | Covered by S16A-S16E. | Universal task escalation, workflow pause-clock, and declarative notification runtime are implemented. | Operations handover, escalated/SLA-breached saved views, and cockpit exception board consume the same evidence contract. | None. | Handover, escalation tasks, breach notifications, watch/blocked/paused state evidence, and audit events are now connected through task/workflow/notification read models. | See S16A-S16E targeted backend/frontend tests. | Pushed | See S16A-S16E verification rows. | `94c245a`, `be6337e`, `87c1936`, `9932f76`, `469be3c` | Pushed to `origin/main`. |
| S17A Approval decision-pack completeness | P0 | JTBD-002, JTBD-051 to JTBD-053 | Approvals existed, but decision-pack completeness was not normalized, projected, or enforced at decision time. | Added computed approval `decision_pack`, fail-closed decision gating, and approval task projection of pack/SLA due evidence. | Approval queue and decision dialog now show missing pack evidence and disable decisions for incomplete packs. | None. | Decision-pack evidence now travels through approval DTOs, task projection attrs, and decision enforcement. | Authority decision-pack, batch approval, task projection, approval API, queue, and dialog tests. | Pushed | `uv run pytest tests/test_authority_decision_pack.py tests/test_batch_approvals.py tests/test_binding_authority_effective.py tests/test_operational_tasks.py -q`; `uv run ruff check app/authority/service.py app/authority/router.py app/authority/views.py app/tasks/projections.py tests/test_authority_decision_pack.py tests/test_batch_approvals.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `npm run test -- approvals-workspace approval-decision-dialog binding-api`; `npm run typecheck`; `git diff --check` all passed. | `3c6dc2f` | Pushed to `origin/main`. |
| S17B Delegation expiry tasking | P0 | JTBD-002, JTBD-054 to JTBD-055 | Effective-authority resolution ignored expired delegations, but active rows past `ends_at` only appeared as admin counts without task/notification remediation. | Added delegation expiry service, worker actor, CLI command, task kind/subject projection, and notification link routing. | Existing task/inbox/admin-delegation surfaces can consume the projected task and `/admin/delegations` notification link. | Added systemd service/timer for scheduled expiry scan. | Expired delegations are marked `expired`, audited, projected to `review_authority_delegation`, and notify grantor/grantee. | Delegation service, task projection, actor registration, notification href, and CLI tests. | Pushed | `uv run pytest tests/test_authority_delegation.py tests/test_operational_tasks.py tests/test_pr15_notifications.py tests/test_w5d_cli.py -q`; `uv run ruff check app/authority/service.py app/tasks/models.py app/tasks/projections.py app/workers/tasks.py app/workers/cli.py app/notifications/router.py tests/test_authority_delegation.py tests/test_operational_tasks.py tests/test_pr15_notifications.py tests/test_w5d_cli.py --select E,F,I,B,SIM`; `git diff --check` all passed. | `556dff3` | Pushed to `origin/main`. |
| S17C Referral-rule governance linkage | P0 | JTBD-002, JTBD-051 to JTBD-055 | Referral-rule admin mutations applied directly and only audited; high-risk changes were not linked to the governed `admin.change` workflow/task queue. | High-risk referral-rule create/update/activate/deactivate now creates governed admin-change tasks with before/after diffs, risk reasons, authority-route warnings, and rollback guidance; admin overview now counts open governed changes. | Referral-rule workspace now mounts the governed admin-change queue for `targetKind="referral_rule"`. | None. | Rule mutations remain audit-recorded and now also create reviewable admin-change workflow/task evidence when risk criteria are met. | Referral-rule governance, admin-change, admin-overview, and admin-change queue tests. | Pushed | `uv run pytest tests/test_binding_admin_referral_rules.py tests/test_admin_change_workflow.py tests/admin/test_overview.py -q`; `uv run ruff check app/underwriting/referral_rules.py app/admin/overview_service.py tests/test_binding_admin_referral_rules.py tests/admin/test_overview.py --select E,F,B,SIM`; `npm run test -- AdminChangeQueue referral-rule-trigger-builder`; `npm run typecheck`; `git diff --check` all passed. | `8d23f3b` | Pushed to `origin/main`. |
| S17 Approval authority packs and delegation | P0 | JTBD-002, JTBD-051 to JTBD-055 | Covered by S17A-S17C. | Decision-pack completeness, delegation expiry tasking, and high-risk referral-rule governance linkage are implemented. | Approval queues/dialogs surface decision-pack evidence; delegation notifications link to admin review; referral-rule admin shows governed changes. | Added delegation-expiry systemd timer/service. | Approval requests, delegation expiry, and admin referral-rule changes now produce task/workflow/notification/audit evidence. | See S17A-S17C targeted backend/frontend tests. | Pushed | See S17A-S17C verification rows. | `3c6dc2f`, `556dff3`, `8d23f3b` | Pushed to `origin/main`. |
| S18A Live facultative placement tasking | P0 | JTBD-056 to JTBD-058, JTBD-060 | Fac placement service existed and the board component existed, but placement work was not projected into operational tasks and the workspace rendered an empty fac board. | Added fac-placement task subject/kind, projection, status-aware available actions, task metadata refresh for live projections, and task completion on bind. | Reinsurance workspace now loads live fac placements, shows subjectivities in detail, refreshes the live board, and gates close/bind actions by placement state. | None. | Create/invite/close capacity now project or refresh `place_facultative`; bind completes the open placement task. | Facultative service/task tests; reinsurance API/workspace tests. | Pushed | `uv run pytest tests/test_facultative_placement.py tests/test_operational_tasks.py -q`; `uv run ruff check app/reinsurance/facultative.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py tests/test_facultative_placement.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `npm run test -- reinsurance-workspace reinsurance-decision-api`; `npm run typecheck`; `git diff --check` all passed. | `61445ed` | Pushed to `origin/main`. |
| S18B Fac placement slip generation and slip tasks | P0 | JTBD-056 to JTBD-058, JTBD-060 | Fac placements had a placeholder generate-slip endpoint and reinsurance slips did not project review work. | Added idempotent fac-placement draft `ReinsuranceSlip` generation linked by `risk_subject_kind/id`, slip task projection, status-aware slip available actions, and task completion on terminal slip outcomes. | Fac placement detail now generates slips through the live API and shows the returned slip reference; API client parses the slip DTO. | None. | Slip create/issue/sign projects `review_reinsurance_slip`; bind/decline/supersede completes open slip review tasks with actor reason evidence. | Facultative slip generation, slip lifecycle, operational task, reinsurance API, and workspace tests. | Pushed | `uv run pytest tests/test_facultative_placement.py tests/test_a2_slip_lifecycle.py tests/test_operational_tasks.py -q`; `uv run ruff check app/reinsurance/facultative.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py tests/test_facultative_placement.py tests/test_a2_slip_lifecycle.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `uv run ruff check app/reinsurance/router_decision.py --select F,I,SIM`; `npm run test -- reinsurance-workspace reinsurance-decision-api`; `npm run typecheck`; `git diff --check` all passed. | `8715709` | Pushed to `origin/main`. |
| S18C Bordereaux mapping memory and review tasks | P1 | JTBD-061 | V2 bordereaux import/validate/reconcile existed, but the workspace sent empty rows, inferred mappings were not durable, and discrepancies did not become work. | Added raw-row import payloads, canonical column mapping inference, tenant-setting mapping memory, validation-error confirm blocking, and bordereau review task projection/completion. | Bordereaux workbench now parses CSV rows through the live API and displays staged row counts, inferred mapping, and remembered-mapping evidence. | No new table; confirmed mapping memory is stored in existing `tenant_settings`, while V2 batch staging remains in-memory. | Import/validate/reconcile project `review_bordereau_batch` for validation warnings/errors or mismatches; confirm completes the open review task after clean validation. | Bordereaux service/task tests; frontend API/workspace tests. | Pushed | `uv run pytest tests/test_bordereaux_v2.py tests/test_operational_tasks.py -q`; `uv run ruff check app/reinsurance/bordereaux_v2.py app/reinsurance/views_bordereaux.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py tests/test_bordereaux_v2.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `uv run ruff check app/reinsurance/router_decision.py --select F,I,SIM`; `npm run test -- reinsurance-workspace bordereaux-api`; `npm run typecheck`; `git diff --check` all passed. | `698a36f` | Pushed to `origin/main`. |
| S18D Treaty renewal gates and economics review tasks | P0 | JTBD-062 to JTBD-063 | Treaty renewal cloned rows but did not start the `treaty.renewal` workflow; treaty cashflows were passive rows and the workspace Cashflows tab was a placeholder. | Treaty renewal now starts and drafts the workflow, projects treaty renewal tasks, exposes treaty workflow actions, assigns stable recompute cashflow IDs, and projects cashflow review tasks from recompute/reinstatement/true-up runs. | Reinsurance workspace now has a treaty Cashflows tab with treaty selection, month selection, list/recompute actions, cashflow table, and open treaty review task evidence. | No new table; cashflow review tasks use deterministic cashflow row IDs for recompute idempotency. | Added `treaty` and `treaty_cashflow` task subjects plus `review_treaty_renewal` and `review_treaty_cashflow` task kinds; workflow state drives treaty available-actions. | Treaty/cashflow task tests, treaty economics endpoint tests, frontend API/workspace tests. | Pushed | `uv run pytest tests/test_operational_tasks.py tests/test_pr05_treaty_economics.py tests/test_pr06_treaty_endpoints.py tests/test_w6i_renewal.py tests/test_w6e_reinstatements.py tests/test_w6f_trueup.py -q`; `uv run ruff check app/reinsurance/service.py app/reinsurance/jobs.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py tests/test_operational_tasks.py tests/test_pr05_treaty_economics.py tests/test_pr06_treaty_endpoints.py --select E,F,I,B,SIM`; `uv run ruff check app/reinsurance/router.py --select F,I,SIM`; `npm run test -- reinsurance-workspace reinsurance-decision-api`; `npm run typecheck`; `git diff --check` all passed. | `15fef50` | Pushed to `origin/main`. |
| S18E Slip/pack discrepancy gates | P0 | JTBD-056 to JTBD-058, JTBD-060 | Slip issue/bind paths did not compare slips against source policy/fac placement terms or required document packs. | Added `compare_slip_pack`, discrepancy acceptance, issue/bind gates, persisted discrepancy evidence, document-role/hash comparison, and disabled slip actions while blocking discrepancies remain unaccepted. | Fac placement detail now shows generated slip pack clear/blocking discrepancy state and can accept reviewed discrepancies through the live API. | No new table; discrepancy evidence and accepted digest live in existing slip terms. | Slip task projection carries pack discrepancy counts/evidence so review queues can prioritize blocked slips. | Slip lifecycle/task tests; frontend API/workspace tests. | Pushed | `uv run pytest tests/test_a2_slip_lifecycle.py tests/test_operational_tasks.py -q`; `uv run ruff check app/reinsurance/service.py app/reinsurance/views.py app/tasks/projections.py app/tasks/service.py tests/test_a2_slip_lifecycle.py tests/test_operational_tasks.py --select E,F,I,B,SIM`; `uv run ruff check app/reinsurance/router.py --select F,I,SIM`; `npm run test -- reinsurance-workspace reinsurance-decision-api`; `npm run typecheck`; `git diff --check` all passed. | `b32e24d` | Pushed to `origin/main`. |
| S18 Reinsurance placement, slips, and treaty economics | P0 | JTBD-056 to JTBD-058, JTBD-060 to JTBD-063 | Covered by S18A-S18E. | Fac placement tasking, fac slip generation, slip review lifecycle, bordereaux mapping memory/review tasks, treaty renewal workflow gates, treaty economics review tasks, and slip/pack discrepancy bind/issue gates are implemented. | Reinsurance workspace now covers live fac placements, slip generation and pack checks, bordereaux V2 import/validation/mapping evidence, and treaty cashflow review/recompute. | None. | Reinsurance placement, slip, bordereaux, treaty renewal, economics, and pack discrepancy work now project operational tasks and expose available-actions where appropriate. | See S18A-S18E targeted backend/frontend tests. | Pushed | See S18A-S18E verification rows. | `61445ed`, `8715709`, `698a36f`, `15fef50`, `b32e24d` | Pushed to `origin/main`. |
| S19A Policy cancellation workflow and review task | P0 | JTBD-068 | Cancellation refund calculation and workflow definition existed, but no protected cancellation API or cancellation review task was wired. | Added `POST /api/policies/{policy_id}/cancel`, cancellation DTOs, workflow evidence persistence, policy execution workflow alignment, and refund/notice review task projection. | Deferred; no frontend files touched because this slice exposes the backend/API task surface first. | None; cancellation evidence is stored in existing policy terms plus workflow/task rows. | `policy.cancellation` and `policy.execution` are advanced during booking; `review_policy_cancellation` tasks carry refund, notice, method, reason, and workflow evidence. | Cancellation service tests, task projection test, route registration, workflow definition regression. | Pushed | `uv run pytest tests/test_policy_cancellation.py tests/test_operational_tasks.py tests/test_api_routes_registered.py tests/test_workflow_definitions.py -q`; `uv run ruff check app/policies/service.py app/policies/router.py app/policies/views.py app/tasks/models.py app/tasks/projections.py app/tasks/router.py tests/test_policy_cancellation.py tests/test_operational_tasks.py tests/test_api_routes_registered.py --select F,I,B,SIM`; `git diff --check` all passed. | `859447c` | Pushed to `origin/main`. |
| S19B Endorsement repricing evidence and approval task | P0 | JTBD-067 | Live endorsement route accepted `new_limit_amount` but ignored it, and endorsement approval tasks carried only basic reference metadata. | Live endorsement creation now computes pro-rata limit-change premium deltas, stores repricing evidence in `terms_diff`, passes the actor into the workflow start, and projects an approval task with limit/premium/term-change evidence. | Deferred; existing endorsement diff panel consumes the backend diff endpoint, while task evidence is backend/API first. | None. | `endorsement.lifecycle` starts with the actor and `approve_endorsement` tasks now carry repricing and diff context for operational review. | Endorsement service repricing test, endorsement diff regressions, operational task projection tests. | Pushed | `uv run pytest tests/test_policy_endorsements.py tests/test_endorsement_diff.py tests/test_operational_tasks.py tests/test_policy_cancellation.py -q`; `uv run ruff check app/policies/service.py app/policies/router.py app/tasks/projections.py tests/test_policy_endorsements.py tests/test_operational_tasks.py --select F,I,B,SIM`; `git diff --check` all passed. | `087e02b` | Pushed to `origin/main`. |
| S19C Renewal pipeline task evidence | P0 | JTBD-071 to JTBD-072 | Renewal queue rows projected basic tasks, but queue/job creation did not start `renewal.pipeline` or expose workflow/rating evidence through operational actions. | Renewal enqueue and pipeline job now start `renewal.pipeline`, persist workflow evidence in `RenewalTask.attrs`, project enriched `process_renewal` tasks, and derive renewal available-actions from the workflow state. | Deferred; frontend can consume the existing task/action APIs and renewal experience panels. | None. | `process_renewal` tasks now carry policy reference, due date, rated premium, pipeline run, workflow id/status, owner assignment, and `awaiting_underwriter` state. | Renewal workflow service tests, renewal experience regressions, operational task/action tests, workflow definition regressions. | Pushed | `uv run pytest tests/test_renewal_workflow.py tests/test_renewal_experience.py tests/test_operational_tasks.py tests/test_workflow_definitions.py -q`; `uv run ruff check app/renewals/service.py app/renewals/jobs.py app/tasks/projections.py app/tasks/service.py tests/test_renewal_workflow.py tests/test_operational_tasks.py --select F,I,B,SIM`; `git diff --check` all passed. | `434b19e` | Pushed to `origin/main`. |
| S19D Material-event affected-record fan-out | P0 | JTBD-070 | Material events existed as CRUD rows and AI cards, but recording an event did not start a workflow, fan out to affected policies, or create review work. | Added `material.event` workflow, affected policy fan-out from explicit policy/client/buyer credit-limit links, material-event task subject/kind, task projection, workflow-backed available-actions, subject summary resolution, and task completion on close. | Deferred; existing AI inbox card and task/action APIs can consume the backend contract first. | None; affected policy ids/count and workflow evidence are stored in existing material-event payload JSON. | `review_material_event` tasks now carry affected-record, deadline, admissibility, and workflow evidence; acknowledge/close keeps workflow payload in sync. | Material-event service/fan-out tests, operational task/action tests, workflow definition tests, route and AI material-event regressions. | Pushed | `uv run pytest tests/test_material_event_workflow.py tests/test_operational_tasks.py tests/test_workflow_definitions.py tests/test_api_routes_registered.py tests/test_ai_assist_services.py::test_material_event_returns_struct -q`; `uv run ruff check app/governance/service.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py app/tasks/router.py app/workflows/definitions/material_event.py app/workflows/definitions/__init__.py tests/test_material_event_workflow.py tests/test_operational_tasks.py tests/test_workflow_definitions.py --select F,I,B,SIM`; `uv run ruff check app/core/listing.py --select F,I,B`; `git diff --check` all passed. | `8358996` | Pushed to `origin/main`. |
| S19 Policy change, renewal, and material-event response | P0 | JTBD-067 to JTBD-068, JTBD-070 to JTBD-072 | Covered by S19A-S19D. | Endorsement repricing evidence, protected cancellation workflow/tasking, renewal workflow/task evidence, and material-event affected-record fan-out are implemented. | Frontend work is limited to existing consumers in this aggregate; backend task/action APIs expose the needed evidence for current work surfaces. | None. | Policy change, renewal, and material-event workflows now create task/workflow evidence and expose available-actions where appropriate. | See S19A-S19D targeted backend tests. | Pushed | See S19A-S19D verification rows. | `859447c`, `087e02b`, `434b19e`, `8358996` | Pushed to `origin/main`. |
| S20A Reserve recompute job evidence and movement review tasks | P1 | JTBD-084, JTBD-095 | Reserve recompute existed as a synchronous claim route and a registered worker actor, but the worker only logged intent, claim recompute returned nonexistent snapshot fields, and material reserve movement did not create review work. | Made the reserve worker compute snapshots, persist `JobRun` and audit evidence, fixed claim recompute output, added reserve total/movement helpers, tenant-configurable `reserve.review.threshold`, and projected material movements into `review_reserve_movement` tasks. | Deferred; existing task/action APIs and case-room recompute button can consume the backend evidence first. | None; uses existing reserve snapshots, tenant settings, job runs, audit events, and operational tasks. | `reserve_snapshot` subjects now expose reserve movement review actions and task summaries; recompute/audit payloads carry snapshot totals and movement projection evidence. | Reserve job/service tests, worker evidence test, operational task projection test, claim recompute response test. | Pushed | `uv run pytest tests/test_pr04_reserving_job.py tests/test_pr10_worker.py tests/test_operational_tasks.py tests/test_claim_reserve_recompute.py -q`; `uv run ruff check app/reserving/service.py app/workers/tasks.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py app/tasks/router.py tests/test_pr04_reserving_job.py tests/test_pr10_worker.py tests/test_operational_tasks.py tests/test_claim_reserve_recompute.py --select F,I,B,SIM`; `uv run ruff check app/claims/router.py app/core/listing.py --select F`; `git diff --check` all passed. | `d3284e1` | Pushed to `origin/main`. |
| S20B Paid-claim recovery-open evidence | P1 | JTBD-086 | Recovery cases could be opened manually, but paid-claim transfer did not automatically create idempotent recovery work, and the service used the wrong recovery workflow key. | Claim `transfer_to_recovery` now opens an idempotent recovery case, starts `recovery.collection`, stamps workflow evidence, writes `recovery.case.opened` audit payloads, and projects recovery-case collection tasks. | Deferred; recovery detail already has timeline support and task/action APIs now expose recovery-case work. | None; uses existing recovery cases, workflow instances, audit events, and operational tasks. | `recovery_case` subjects now expose workflow-backed actions and `advance_recovery` tasks from the opened case. | Recovery-open workflow tests, operational task projection tests, workflow definition regressions, route registration. | Pushed | `uv run pytest tests/test_recovery_open_workflow.py tests/test_operational_tasks.py tests/test_workflow_definitions.py tests/test_api_routes_registered.py -q`; `uv run ruff check app/recovery/service.py app/claims/service.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py app/tasks/router.py tests/test_recovery_open_workflow.py tests/test_operational_tasks.py --select F,I,B,SIM`; `git diff --check` all passed. | `2efaf48` | Pushed to `origin/main`. |
| S20C CAT workflow fan-out evidence | P1 | JTBD-085 | CAT tagging linked a claim to a CAT event and wrote a claim event, but there was no CAT workflow, aggregate review task, subject action surface, or CAT-level audit evidence. | Added `cat.event` workflow, CAT event subject/action support, aggregate `review_cat_event` task projection, CAT subject summaries, and CAT-level audit payloads on claim tagging. | Deferred; existing tasks and subject available-actions expose CAT review work first. | None; uses existing CAT event rows, claim aggregates, workflow instances, audit events, and operational tasks. | Tagging a claim now starts/updates CAT workflow evidence and projects aggregate claim-count/loss review work. | CAT service tests, operational task projection tests, workflow definition regressions, route registration. | Pushed | `uv run pytest tests/test_w7c4_cat.py tests/test_operational_tasks.py tests/test_workflow_definitions.py tests/test_api_routes_registered.py -q`; `uv run ruff check app/claims/service.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py app/tasks/router.py app/core/listing.py app/workflows/definitions/cat_event.py app/workflows/definitions/__init__.py tests/test_w7c4_cat.py tests/test_operational_tasks.py tests/test_workflow_definitions.py --select F,I,B,SIM`; `git diff --check` all passed. | `433978d` | Pushed to `origin/main`. |
| S20D Scheduled report dispatch evidence | P1 | JTBD-095 to JTBD-096 | Scheduled reports rendered and emailed, but per-report dispatches were only logged and did not persist job/audit evidence for rendered artifact metadata, recipients, failures, or next run state. | Wrapped scheduled report dispatch with `JobRun` and audit evidence for success/failure, rendered filename, content size, recipient count, and schedule advancement. | Deferred; existing notifications/jobs/admin surfaces can consume durable job/audit evidence first. | None; uses existing scheduled reports, job runs, audit events, and notification dispatch. | Scheduled report exports now leave durable job/audit evidence in addition to notification delivery. | Scheduled report worker evidence test and existing scheduled-report notification dispatch test. | Pushed | `uv run pytest tests/test_pr10_worker.py tests/test_pr15_notifications.py::test_dashboards_dispatch_to_recipients_uses_send_with_user_id_none -q`; `uv run ruff check app/workers/tasks.py tests/test_pr10_worker.py --select F,I,B,SIM`; `git diff --check` all passed. | `5a4b8f4` | Pushed to `origin/main`. |
| S20 Reserving, CAT, recovery-open, and report evidence | P1 | JTBD-084 to JTBD-086, JTBD-095 to JTBD-096 | Covered by S20A-S20D. | Reserve recompute worker execution, reserve movement review tasks, paid-claim recovery-open workflow/task/audit evidence, CAT workflow fan-out, and scheduled report job/audit evidence are implemented. | No dedicated frontend files touched in this aggregate; current case-room, recovery detail, task/action, notification, and job/audit surfaces can consume the backend evidence. | None. | Reserve jobs, recovery-open actions, CAT tags, and scheduled report exports now produce task/workflow/job/audit evidence where appropriate. | See S20A-S20D targeted tests. | Pushed | See S20A-S20D verification rows. | `d3284e1`, `2efaf48`, `433978d`, `5a4b8f4` | Pushed to `origin/main`. |
| S21 Recovery partner and proceeds lifecycle | P1 | JTBD-087 to JTBD-090, JTBD-114 | Recovery collection existed, but partner assignment, strategy/cooperation, reports, fees, and proceeds sharing were not first-class evidence. | Added partner assignment, recovery strategy/cooperation, partner report/fee, and proceeds-sharing receipt APIs; refreshed recovery operational tasks with partner/strategy/proceeds context and audit events. | Recovery detail now exposes partner assignment, strategy/cooperation, partner report, and receipt/proceeds controls using the new API helpers. | None; extended lifecycle evidence is stored in existing recovery `attrs` and task/audit rows. | Partner updates, reports, receipts, fees, and proceeds sharing now audit and refresh recovery work; write-off authority gate remains through the existing approval-backed write-off route. | Recovery partner/workflow tests, route registration, task projection tests, frontend lint. | Pushed | `uv run pytest tests/test_recovery_open_workflow.py tests/test_operational_tasks.py tests/test_api_routes_registered.py -q`; `uv run ruff check --select E,F,I app/recovery/service.py app/recovery/router.py app/recovery/views.py app/tasks/projections.py tests/test_recovery_open_workflow.py tests/test_api_routes_registered.py`; `npm run lint -- --file src/app/recovery/[id]/page.tsx --file src/lib/api.ts`. Frontend typecheck is blocked by pre-existing dirty reinsurance test errors. | `41f3327`, `1698530` | Pushed to `origin/main`. |
| S22 Jobs, observability, and inbound integration actionability | P0 | JTBD-106 to JTBD-107, JTBD-115 | Jobs, observability, and inbound events existed, but inbound events were raw log rows and the jobs/observability frontend was a generic wrapper. | Inbound events now project `review_inbound_event` admin tasks, write inbound audit events, resolve subject summaries, and integration health counts `WebhookDelivery.state` correctly. | Jobs page now exposes workers, queues, registered actors, failed DLQ retry/cancel controls; observability page consumes `/ops/health` and `/ops/version` for dependency/build state. | None; uses existing inbound events, webhook deliveries, operational tasks, DLQ, worker heartbeats, and ops endpoints. | DLQ retry/cancel controls, worker/queue status, health dependencies, and inbound events are now visible/actionable through task or operator surfaces. | Inbound webhook/actionability tests, admin integration health test, operational task regressions, frontend lint. | Pushed | `uv run pytest tests/test_pr15_inbound_webhooks.py tests/admin/test_integrations.py tests/test_operational_tasks.py -q`; `uv run ruff check app/integrations/service.py app/admin/integrations_admin_service.py app/tasks/models.py app/tasks/projections.py app/tasks/service.py app/core/listing.py tests/test_pr15_inbound_webhooks.py tests/admin/test_integrations.py --select F,I,B,SIM`; `npm run lint -- --file src/app/jobs/page.tsx --file src/app/observability/page.tsx --file src/lib/jobs-api.ts`. Frontend typecheck remains blocked by pre-existing dirty reinsurance test errors. | `0c77ddb`, `043dc2e` | Pushed to `origin/main`. |
| S23 Security, compliance, and capability governance | P0 | JTBD-117 to JTBD-119 | Security and feature flags existed, but legal hold was not first-class, high-risk admin changes were not all reason-gated, and available-actions did not explain capability-disabled actions. | Added document legal-hold/release services, purge protection, audit events, document review tasks, reason enforcement for admin flag/settings/secret mutations, and capability-aware available-action gates. | Existing task/action panels now surface legal-hold review tasks and disabled capability reasons; no separate compliance page was required for this slice. | None; legal hold uses existing document attrs and operational tasks. | Legal hold blocks purge, high-risk admin/capability mutations require `X-Reason`, and mapped workflow actions are disabled with clear tenant-capability reasons when flags are explicitly off. | Legal hold/retention tests, reason middleware tests, operational task capability tests. | Pushed | `uv run pytest tests/test_w7g4_retention.py tests/test_operational_tasks.py -q`; `uv run pytest tests/test_audit_reason_middleware_extended.py -q`; `uv run pytest tests/test_operational_tasks.py -q`; matching ruff focused checks all passed. | `3469405`, `659773e`, `b86349e` | Pushed to `origin/main`. |
| S24 Strategic marketplace/accounting boundaries | P3 | JTBD-120 to JTBD-121 | Strategic; catalogue marks these as roadmap/out-of-scope for full implementation. | Added `queue_domain_event` as the tenant-scoped external event boundary for finance/partner/accounting handoffs without implementing marketplace or GL state. | Catalogue guardrails now state that MVP workflow completion does not include a public marketplace, capacity exchange, GL, IFRS 17 subledger, or actuarial pricing engine. | None; uses existing webhook endpoint/delivery tables. | Finance/reinsurance/partner workflows can emit integration-ready events with tenant, subject, action, actor, audit, and payload context. | Domain event boundary contract test and focused ruff. | Pushed | `uv run pytest tests/test_s24_domain_event_boundary.py -q`; `uv run ruff check app/integrations/service.py tests/test_s24_domain_event_boundary.py --select F,I,B,SIM`; `git diff --check` passed. | `e0218da` | Pushed to `origin/main`. |

## Current Ralph State

- Branch: `main`
- Context snapshot: `.omx/context/jtbd-workflow-support-20260430T124721Z.md`
- PRD: `.omx/plans/prd-jtbd-workflow-support.md`
- Test spec: `.omx/plans/test-spec-jtbd-workflow-support.md`
- Next action: final Ralph verification, architect review, deslop pass, and state cleanup.

## S01 Baseline Findings

- The feature branch `jtbd-workflow-support` was created from `main`.
- The worktree already contained many uncommitted workflow-hardening changes before this JTBD Ralph run. These include workflow idempotency/locking, next-action read contracts, paused workflow deadline handling, ingestion workflow synchronization, SLA breach scan changes, task action derivation, and related tests.
- Those pre-existing code changes were inspected but not claimed as completed JTBD implementation in S01.
- S01-owned artifacts are the JTBD Ralph context, PRD, test spec, implementation tracker, and catalogue/backup docs.
- `git diff --check` passed for the current worktree.
