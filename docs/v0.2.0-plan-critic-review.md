# Critic Review — FlowForge v0.2.0 Plan v1

**Reviewer role:** Critic (opus tier, strict-gate evaluator)
**Subject:** `framework/docs/v0.2.0-plan.md` (v1, post-architect)
**Predecessor reviews:** `framework/docs/v0.2.0-plan-architect-review.md` (WITH-REVISIONS, 21 must-fix)
**Review date:** 2026-05-08
**Mode:** DELIBERATE — adversarial gate evaluation

---

## §1 Verdict rationale

v1 absorbs the architect's 21 must-fix items with genuine substance. The V-1 BLOCKER (self-review masquerading as SME validation) is properly struck: Option B's "same Claude agent acting as 30 SMEs" language is gone from §1.3, Tier-A/Tier-B distinction is in the principles, and the signoff block at §15 carries machine-readable `reviewer_credentials: ai-author`, `tier: B`, and `human_sme_review: pending` fields. The 5 open questions from v0 §13 are answered, not posed. The critical-path math is corrected (6 wk, X1→X2→X3 sequential, no idle gap). The 8-scenario pre-mortem is present. All 8 invariants are required-green per X3 PR.

However, three concrete deficiencies survive architect-review absorption, two of which are MAJOR:

1. **Ticket ID collision (MAJOR):** `E-78b` is used for two distinct work items — the engineering snapshot ticket (§5.1, X2 sprint) and the second Tier-B content batch (v0.2.5, §4.2). An executor following this plan will have ambiguous target for every CI reference to `E-78b`. This is an unambiguous naming error that will corrupt signoff-checklist row lookup, `check_signoff.py` output, and PR tagging.

2. **Soak runner file referenced but does not exist (MAJOR):** `make v020-soak-forks` calls `uv run python framework/tests/soak/run_soak.py`, but `framework/tests/soak/` does not exist in the repository. Gate 7 (the X3 hard exit gate) depends on this script. Referencing a non-existent file as a hard exit gate means the gate is unrunnable as written — G-5 fails.

3. **Tier-A named SME signer is a `<name>` placeholder throughout (MINOR):** §5.2 Tier-A acceptance demands `signer: <name>` and §4.2 requires "named insurance SME + product lead sign" for v0.2.1. The plan acknowledges single-stakeholder governance (audit-2026 signoff-checklist roles header), but the specific human identity for each of the 5 Tier-A domain SMEs is not committed in the plan. The audit-2026 precedent named Nyimbi Odero in every SME role slot at signoff time. This is a MINOR because audit-2026 established that single-stakeholder governance is acceptable and documented, not a structural gap — but the plan should explicitly state which human fills each Tier-A SME slot at sprint kickoff rather than leaving `<name>` as a deferred placeholder.

No new BLOCKERs survive. All gates are evaluated below.

**Verdict: ITERATE** — one Partial-that-is-actually-a-naming-error (E-78b collision) and one non-existent soak script file must be resolved before this plan is executable. These are concrete, small fixes. v2 should be achievable in a single pass.

---

## §2 Critic gate evaluation matrix

| Gate | Result | Evidence |
|---|---|---|
| **G-1: Principle-option consistency** | Pass | P-1..P-6 are each defended by Option C. Cons of Option C (two-class catalogue, registry dependencies, semver complexity) are stated and do not contradict any principle. Option B rejected explicitly with architect V-1 reference. |
| **G-2: Fair alternatives** | Pass | Options A, B, C each have pros AND cons columns. Option B rejection cites architect V-1 BLOCKER + T-1. Option A rejection cites elapsed time + content-coupling. No option is presented without a con line. |
| **G-3: Risk mitigation clarity** | Pass | R-1..R-8 each carry: concrete action (not "triage"), named owner role (engine-track owner / security lead / CI-test eng / observability/SRE / release manager), detection signal (conformance red / wall-clock threshold / "no data" return / `uv.lock` diff size), rollback path. All 8 map to a pre-mortem scenario. |
| **G-4: Testable acceptance criteria per ticket** | Pass | Every E-75..E-82 + E-78b (engineering) + R-01..R-07 (registries) row has explicit `test_id` strings, file paths, and concrete green criteria. Content tickets have `test_D_01_<domain>_*` patterns with concrete counts. No "pattern is uniform" hand-waves. |
| **G-5: Concrete verification steps** | Partial/Fail | `make v020-soak-forks` calls `framework/tests/soak/run_soak.py` — this file does not exist. The soak runner is Gate 7, the X3 hard exit gate. All other make targets reference plausible paths (`framework/tests/conformance/`, `framework/python/flowforge-jtbd-hub/tests/`). One non-existent path makes G-5 a Partial. Evidence: directory confirmed absent via `ls /Users/nyimbiodero/src/pjs/ums/framework/tests/soak/` → "DIR NOT FOUND". |
| **G-6: DELIBERATE pre-mortem completeness** | Pass | 8 scenarios F-1..F-8. Each has trigger, detection, mitigation, rollback, owner. F-6/F-7/F-8 are the three additions the architect demanded (must-fix #4). Upper bound of 7 exceeded (8), appropriate for DELIBERATE mode + 6-wk invariant-touching sprint. |
| **G-7: 7+ row risk register with named owners** | Pass | R-1..R-8 (8 rows). Each maps to a pre-mortem F-n. Each owner is a role (not "TBD"): engine-track owner, security lead, CI/test eng, observability/SRE eng, release manager. Rollbacks are specific (`git revert <sha>`, `flag stays off`, `reduce TTL to 10s`). |
| **G-8: Tier-A/Tier-B BLOCKER resolution** | Pass | (a) V-1 line is struck — `v0.2.0-plan.md:55` Option B con-line reads "30 SMEs = 1 Claude agent in 30 personas without named-human review (architect V-1 BLOCKER)" as the rejection reason, not as a valid model. (b) §15 signoff block has `reviewer_credentials: ai-author`, `tier: B`, `human_sme_review: pending` — machine-readable. (c) §5.2 Tier-A acceptance demands `signer: <name>`, `reviewer_credentials: <SME role>` per JTBD. Partially weakened by `<name>` placeholder (see CR-2, MINOR). |
| **G-9: Engineering critical path math** | Pass | X1 = Wks 1–2, X2 = Wks 3–4, X3 = Wks 5–6. Sequential, no idle gap. §8 states `X1.E-73 phase 4-6 → X2.E-78b + R-01..R-07 → X3.E-79 → X3.E-80 → X3.E-81 → X3.E-82 = 6 wk`. §7 effort table: X1 2.0 pwk + X2 1.5 pwk + X3 2.5 pwk = 6.0 pwk over 6 wk. Math is consistent and internally coherent. V-4 MINOR (semver anchoring) resolved: `forks_enabled` default-off in v0.2.0, default-on in v0.3.0 only after v0.2.x soak window. |
| **G-10: §13 questions answered** | Pass | §2 contains a 5-row table explicitly answering Q-1..Q-5 with source citations (architect §6.5–§6.9 + must-fix references). No new open questions appear in v1. |
| **G-11: Audit-2026 close-out criteria as exit gates** | Pass | §6.3 explicitly restates criterion 7 (24h soak) and criterion 8 (per-fix observability) as v0.2.0 hard exit gates with fail=block-release language. `make v020-soak-forks` named as X3 hard gate (modulo G-5 soak script path deficiency). Gate 8 tied to X1 ship gate. |
| **G-12: Pre-X2 cross-domain registries committed** | Pass | §5.1 R-01..R-07 row: all 7 registries enumerated with concrete file paths (`flowforge_jtbd/registries/{citations,channels,roles,doc_types,jtbd_ids,edge_case_ids,sla_keys}.yaml`), acceptance test IDs, and "pre-X2 dependency for any v0.2.x Tier-B drafting" stated. E-47 lint solver extension included. |

**Summary: 10 Pass, 1 Partial (G-5), 1 Pass-with-minor (G-8). No outright Fails.**

---

## §3 Findings — what v2 must add

### Must-fix (blocks APPROVE)

| ID | Section | Required change |
|---|---|---|
| **CR-1** | §4.1, §4.2 (sprint table v0.2.5) | **Rename E-78b (engineering ticket) to E-78e** or any unambiguous ID. `E-78b` currently identifies two distinct work items: the engineering `Instance.tokens` snapshot ticket (§5.1 acceptance table, §4.1 X2 sprint, §8 critical path, §13 signoff sample) AND the Tier-B content batch for v0.2.5 (ecom, edu, gaming, legal, logistics). Every downstream reference — `check_signoff.py` row lookup, PR tagging, CI ratchet output — will be ambiguous. Rename the engineering ticket to `E-78e` (or `E-Ph1`) throughout, OR rename the Tier-B v0.2.5 batch to `E-78-batch2`. Either direction is acceptable; consistency across all 10+ plan references is required. |
| **CR-2** | §6.2 (`make v020-soak-forks`), §6.3 Gate 7 | **Either create `framework/tests/soak/run_soak.py` as a plan deliverable (with its own ticket in X3), or reference the existing soak runner from audit-2026** (`scripts/ops/audit-2026-soak.sh`) with the `--workflow` flag extension as the X3 implementation target. The current invocation `uv run python framework/tests/soak/run_soak.py --rate=10 --workflow=test_flow_3_parallel_fork` calls a file that does not exist. Gate 7 is the X3 hard exit gate; it must be runnable as written. Add a line to §5.1 E-82 or X3 sprint scope: "extend `scripts/ops/audit-2026-soak.sh` with `--workflow` flag and `--forks-enabled` flag, OR create new `framework/tests/soak/run_soak.py`." |

### Should-fix (strengthens plan, not blocking)

| ID | Section | Required change |
|---|---|---|
| **CR-3** | §4.2 (v0.2.1..v0.2.3), §5.2 Tier-A acceptance | At sprint kickoff, name the specific human filling each of the 5 Tier-A domain SME slots (insurance SME, healthcare SME, banking SME, gov SME, hr SME). The plan currently has `signer: <name>` and "named insurance SME + product lead sign" without committing identities. The audit-2026 signoff-checklist resolved this by naming Nyimbi Odero in the roles header at the start of the sprint. v2 should carry the same resolution — either name the humans explicitly in the plan, or commit to naming them in `framework/docs/v0.2.0/signoff-checklist.md` roles header before X4 (content sprint) starts. |
| **CR-4** | §4.2 (v0.2.7), §14 backlog | The plan notes that `platformeng`, `travel`, and `utilities` are in scope for the v0.2.7 decision-point review but are not explicitly added to the deferred/backlog list. The architect's Appendix D rated `platformeng` and `travel` as Tier-B and `utilities` as very-high citation risk ("NERC CIP citations are critical-infrastructure-grade"). v2 should either explicitly include them in a v0.2.7 batch or add them to §14 with a rationale, so an executor has no ambiguity about whether these 3 domains are in scope for v0.2.7 delivery. Currently v0.2.7 says "scope review for remaining domains (compliance, municipal, platformeng, travel, utilities)" — that is a review, not a commitment. |

---

## §4 Detailed gate-by-gate findings

### G-1: Principle-option consistency

All 6 principles are defended by Option C:

- P-1 (audit-2026 quality template) → Option C preserves Tier-A at full bar, relaxes to Tier-B with explicit labelling. Option C con-line "two-class catalogue requires consumer-facing labelling discipline" acknowledges the tension and the plan resolves it via README + machine-readable metadata.
- P-2 (named human SMEs for Tier-A) → Option C explicitly decouples Tier-A human signoff from Tier-B AI authorship.
- P-3 (three frozen schemas + 7 registries) → Option C's pre-X2 deliverable set is the registries; X2 starts only after.
- P-4 (layered feature flag) → Option C sequences engine wiring entirely in X3, default-off.
- P-5 (every deliverable with signoff row) → Option C's per-sprint DELIBERATE signoff column is populated.
- P-6 (staged releases) → Option C is the staged-release option.

No principle contradiction detected.

### G-2: Fair alternatives

Option A: pros (simpler coordination) AND cons (12 wk, blocks content, misses parallelism guidance). Rejection rationale: elapsed time + content coupling. Sound.

Option B: pros (one announcement, parallel teams) AND cons (coupling SME pace to engine release, F-5 re-introduction, V-1 BLOCKER). Rejection rationale: architect V-1 + T-1. Sound, and the rejection is more specific in v1 than v0.

Option C: pros (decouples cadences, inherits F-5 mitigation, honest labelling) AND cons (two-class catalogue, registry dependencies, semver coordination). Chosen with rationale.

All three options have both pro and con columns. G-2 passes.

### G-3: Risk mitigation clarity

R-1: Action = "per-domain citation library (R-01 registry) + disclaimer"; Owner = "per-domain SME (Tier-A); release manager + AI-content reviewer (Tier-B)"; Detection = "citation library audit; downstream consumer issue"; Rollback = "revert offending pkg commit." Concrete, not "triage."

R-2: Action = "layered flag + required-green all 8 invariants every PR + 24h soak hard gate"; Owner = "engine-track owner"; Detection = "conformance suite red on PR"; Rollback = "git revert; flag stays off." Critical-path risk has the strongest mitigation.

R-3..R-8: Each meets the standard. R-8 (revocation propagation) explicitly documents the SLA rather than pretending it away — this is the correct pattern established by F-8 pre-mortem.

### G-5: Concrete verification steps — the soak gap (MAJOR)

The makefile target at §6.2:

```makefile
v020-soak-forks:
    FLOWFORGE_FORKS_ENABLED=1 FLOWFORGE_SOAK_DURATION_HOURS=24 \
        uv run python framework/tests/soak/run_soak.py --rate=10 --workflow=test_flow_3_parallel_fork
```

`framework/tests/soak/` does not exist. `scripts/ops/audit-2026-soak.sh` does exist (close-out.md criterion 7 confirmed it landed). The simplest fix is to extend that existing shell script with fork-workflow support rather than creating a new Python soak runner from scratch. Either way, the plan must name a file that exists or commit to creating it in a specific ticket.

All other make targets (`v020-content-lint`, `v020-engine-flag-on`, `v020-jwt-fuzz`, `v020-cross-domain-conflict-lint`) reference plausible existing directory structures. Only the soak runner is missing.

### G-6: Pre-mortem completeness

F-1..F-8 all have the required five columns (trigger, detection, mitigation, rollback, owner). F-6 (signing-kms boot crash) introduces lazy initialization as the mitigation — this is testable (the test is `test_E_76_jwt_*` suite). F-7 (workspace flip churn) introduces the 5-pkg-per-minor cap with a ratchet — the ratchet `uv_lock_diff_size.sh` is named and added to §6. F-8 (revocation 30s window) documents the SLA rather than eliminating it, which is the correct threat-model posture.

Pre-mortem passes.

### G-8: BLOCKER resolution verification

Three sub-checks the architect demanded for V-1:

**(a) The "same Claude agent as 30 SMEs" line struck?** Yes. `v0.2.0-plan.md:55` (Option B con-line) mentions it as the rejection reason: `"30 SMEs" = 1 Claude agent in 30 personas without named-human review (architect V-1 BLOCKER)`. The line is no longer presented as an acceptable approach — it is the grounds for rejection.

**(b) Machine-readable `reviewer_credentials: ai-author` + `tier: B` + `human_sme_review: pending` in signoff block?** Yes. `v0.2.0-plan.md:476-481`:
```yaml
signoff:
  signer: ai-author
  date: 2026-XX-XX
  reviewer_credentials: ai-author
  citation_library_version: R-01@v0.2.0
  tier: B
  human_sme_review: pending
```
All three machine-readable fields present.

**(c) Tier-A acceptance demands a named human SME signer?** Yes, but with placeholder: `signer: <name>`, `reviewer_credentials: <SME role>` in §5.2. The plan names the role but not the identity. Audit-2026 resolved this in the signoff-checklist roles header at sprint start. v1 does not commit the identity yet (see CR-3, MINOR).

Overall: BLOCKER V-1 is resolved at the structural level. The placeholder identities are a MINOR gap, not a BLOCKER recurrence.

### G-9: Critical path math

v0 had the fiction: "X1.E-73 phase 4-6 → X3.E-79 → … = ~5.5 wk" with X3 scheduled at wks 7–9 in the sprint table (leaving a 4-wk idle gap).

v1 states: X1 = Wks 1–2, X2 (E-78b + registries) = Wks 3–4, X3 = Wks 5–6. Total = 6 wk, no idle gap. The architectural concern in architect §6.2 is resolved. The effort table (§7) confirms: 2.0 + 1.5 + 2.5 = 6.0 engineering person-weeks over 6 calendar weeks.

The E-78b engineering snapshot prerequisite (v0 was missing it entirely) now occupies its correct position at X2, sequenced before E-79. Invariant 3 (replay determinism) is protected by this sequencing.

### G-10: §13 questions answered

All 5 questions closed in §2 with explicit answers, source citations, and architect §6.x mappings. No new open questions introduced in v1. The "§13" label is repurposed in v1 as a README disclaimer template appendix — clean closure of the open-question section.

### G-11: Audit-2026 exit gates

§6.3 explicitly cites criteria 7 and 8, uses "Fail = block release" language, and ties them to specific sprints (Gate 7 = X3; Gate 8 = X1). Criterion 8 is tied to `flowforge audit-2026 health --json` returning non-stub data, which is a specific, testable command. Criterion 7 is tied to `make v020-soak-forks` — which has the path deficiency noted in G-5/CR-2.

### G-12: Cross-domain registries

All 7 registries explicitly named in §5.1 R-01..R-07 row: citations, channels, roles, doc_types, jtbd_ids, edge_case_ids, sla_keys. File paths committed to `framework/python/flowforge-jtbd/src/flowforge_jtbd/registries/<name>.yaml`. Per-registry test IDs named. Sequenced as pre-X2 deliverables. The lint-conflict solver (E-47) extension is included. G-12 passes.

---

## §5 Anti-patterns spotted in v1

| Anti-pattern | Where | Resolution |
|---|---|---|
| **Ticket ID reuse** | `E-78b` names both the engineering Instance.tokens ticket (§5.1, §4.1, §8) and the Tier-B content batch 2 (§4.2 v0.2.5 row). | Rename one. See CR-1. |
| **Non-existent file as hard exit gate** | `framework/tests/soak/run_soak.py` in `v020-soak-forks` make target. | Either create the file (with ticket) or reference `scripts/ops/audit-2026-soak.sh`. See CR-2. |
| **E-75..E-84 signoff claim vs E-83/E-84 deferred** | §0 header says "Every E-75..E-84 row carries the audit-2026 close-out schema" but E-83/E-84 are deferred to v0.3.0 in §14 (backlog). The "E-75..E-84" range reference is stale — E-83/E-84 do not exist in v0.2.0. | Update §0 to read "E-75..E-82 + E-78b" to match the actual engineering ticket range. |
| **X4 label used in two senses** | In §4.1 sprint table, X4 does not appear (sprint table has X1/X2/X3 only for v0.2.0). But §6.1 test plan table uses "X4 (every Tier-A and Tier-B drop)" to mean the rolling-content phase. §5.2 uses "X4 across v0.2.1..v0.2.7". This is consistent within v1 but differs from v0's X4 (which was the UI/CLI sprint). Add a one-line definition of X4 = "rolling-content execution phase (v0.2.1..v0.2.7)" to §4 to prevent executor confusion. |
| **3-role signoff vs single-stakeholder reality** | §13 signoff schema demands 3 named signers (architecture lead + release manager + security lead) with `signer: <name>` placeholders. The audit-2026 signoff-checklist resolved this via a "single-stakeholder approval pattern" note in the roles header. v1 does not carry that note in the v0.2.0 signoff schema, creating an apparent requirement for 3 distinct humans that the project cannot satisfy. The note should be ported. |

---

## §6 Critic acceptance test for v2

For v2 to pass critic gate (APPROVE), the v2 plan must:

1. Rename one of the two `E-78b` uses consistently throughout all occurrences (§0, §4.1 X2 sprint, §5.1 acceptance table, §6.1 test plan table, §7 effort table, §8 critical path, §11.1, §13 signoff sample, §13 `atomic_fix_proof` text, §13 pre_deploy_checks). Minimum 10 occurrences must be updated to the chosen new ID.
2. Either (a) add a ticket in X3 scope to create `framework/tests/soak/run_soak.py` (with acceptance test ID and file path), or (b) update `make v020-soak-forks` to call `scripts/ops/audit-2026-soak.sh --forks-enabled --workflow test_flow_3_parallel_fork --duration 24h --rate 10` and add a ticket to extend that shell script with the required flags.
3. Carry the audit-2026 "single-stakeholder approval pattern" note in the `framework/docs/v0.2.0/signoff-checklist.md` roles header (or reference the note explicitly in §13), so the 3-role signoff requirement is reconciled with single-stakeholder governance.
4. Fix the `E-75..E-84` range claim in §0 to `E-75..E-82 + E-78b` (or the renamed engineering ticket ID).
5. Add a one-line X4 definition to §4 clarifying X4 = rolling-content execution phase.

Items 1 and 2 are required. Items 3, 4, 5 are should-fix for a clean v2.

If v2 satisfies items 1 and 2, all 12 gates will Pass (with G-8 moving from Partial to full Pass once single-stakeholder note is carried). That is the APPROVE threshold.

---

## §7 Verdict

**Verdict: ITERATE**

v1 is structurally sound. The architect's BLOCKER is resolved. The 21 must-fixes are absorbed with substance, not lip service. The plan would be approvable but for two concrete defects: a ticket ID collision that makes the signoff machinery ambiguous (CR-1), and a non-existent soak runner file that makes the X3 hard exit gate unexecutable (CR-2). Both are small, focused fixes — not structural rewrites. v2 should be a targeted patch, not a full plan revision.

The 10 passing gates and the BLOCKER resolution provide high confidence that v2 will APPROVE. This is loop 1 of the consensus cycle; the loop should converge at v2.

---

*End of critic review. Planner addresses CR-1 (ticket ID rename) and CR-2 (soak runner path) before resubmit.*
