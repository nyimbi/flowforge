# {{ project.name }} environment defaults.
# Copy to .env and fill in real values before running uvicorn.

DATABASE_URL=postgresql+asyncpg://localhost:5432/{{ project.package }}
APP_TENANT_DEFAULT=default
