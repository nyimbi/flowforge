# {{ jtbd.title }} — operator manual

> Generated by `flowforge jtbd-generate` from the JTBD bundle. Re-run the
> generator after editing the bundle; this file is regenerated end-to-end
> and should not be edited by hand.

| | |
|---|---|
| **JTBD id** | `{{ jtbd.id }}` |
| **Actor role** | `{{ jtbd.actor_role }}` |
| **Project** | {{ project.name }} |

## Introduction

**Situation.** {{ jtbd.situation }}

**Motivation.** {{ jtbd.motivation }}

**Outcome.** {{ jtbd.outcome }}

## How to know it worked

{% if jtbd.success_criteria %}
{% for criterion in jtbd.success_criteria %}
{{ loop.index }}. {{ criterion }}
{% endfor %}
{% else %}
_No success criteria declared in the bundle._
{% endif %}

## State diagram

The synthesised state machine for `{{ jtbd.id }}` is rendered below as a
mermaid `stateDiagram-v2`. The canonical deterministic source lives at
[`../../workflows/{{ jtbd.id }}/diagram.mmd`](../../workflows/{{ jtbd.id }}/diagram.mmd)
and is the single source of truth; hosts that want SVG / PNG output run
`mmdc -i workflows/{{ jtbd.id }}/diagram.mmd -o diagram.svg` themselves
on the mermaid source.

```mermaid
{{ diagram_mmd -}}
```

## Form

The customer-facing form rendered for `{{ jtbd.id }}` captures
{{ jtbd.fields | length }} field{% if jtbd.fields | length != 1 %}s{% endif %}:

{% if field_bullets %}
{% for bullet in field_bullets %}
{{ bullet }}
{% endfor %}
{% else %}
_No data-capture fields declared in the bundle._
{% endif %}

Live rendering: see the generated frontend at
[`../../frontend/`](../../frontend/). The static form-spec source lives
at
[`../../workflows/{{ jtbd.id }}/form_spec.json`](../../workflows/{{ jtbd.id }}/form_spec.json).

Visual-regression baselines (when present) live under
`../../../screenshots/frontend/Step.<viewport>.png` per the framework's
W3 visual-regression invariants (mobile / tablet / desktop). When the
baseline is missing the renderer shows a broken-image fallback; that is
expected for any bundle whose hosting tree has not yet committed
Playwright screenshots. The image embed below resolves automatically once
the baseline lands:

![{{ jtbd.title }} — desktop form rendering](../../../screenshots/frontend/Step.desktop.png)

## Audit topics

These audit topics fire during the JTBD's lifecycle. The audit-pg
adapter chain-verifies each topic at restore time. The cross-bundle
canonical catalog lives at
[`../../backend/src/{{ project.package }}/audit_taxonomy.py`](../../backend/src/{{ project.package }}/audit_taxonomy.py).

{% if audit_lines %}
{% for topic, summary in audit_lines %}
- **`{{ topic }}`** — {{ summary }}
{% endfor %}
{% else %}
_No audit topics declared for this JTBD._
{% endif %}

## Permissions

Operators need the following permissions to drive `{{ jtbd.id }}`
end-to-end. The full per-bundle permission catalog lives at
[`../../backend/src/{{ project.package }}/permissions.py`](../../backend/src/{{ project.package }}/permissions.py).

{% if permission_lines %}
{% for perm, summary in permission_lines %}
- `{{ perm }}` — {{ summary }}
{% endfor %}
{% else %}
_No JTBD-scoped permissions declared (coverage comes from `shared.permissions`)._
{% endif %}
