// Next.js app-router page for the {{ jtbd.title }} workflow.
// Generated by flowforge JTBD generator.
import * as React from "react";
import { {{ jtbd.class_name }}Step } from "@/components/{{ jtbd.url_segment }}/{{ jtbd.class_name }}Step";

export const dynamic = "force-dynamic";

export default function {{ jtbd.class_name }}Page(): React.ReactElement {
	const [state, setState] = React.useState("{{ jtbd.initial_state }}");
	const instanceId = "demo";

	const onEvent = React.useCallback(
		async (event: string, _payload?: Record<string, unknown>): Promise<void> => {
			// Generated stub — host swaps this for a real fetch to /api/{{ jtbd.url_segment }}/events.
			if (event === "submit") setState("review");
			else if (event === "approve") setState("done");
		},
		[],
	);

	return (
		<main>
			<h1>{{ jtbd.title }}</h1>
			<{{ jtbd.class_name }}Step instanceId={instanceId} state={state} onEvent={onEvent} />
		</main>
	);
}
