"""Alembic migration: create ``{{ jtbd.table_name }}`` for {{ jtbd.title }}.

Generated by flowforge JTBD generator — deterministic revision id.
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = "{{ revision }}"
down_revision = None
branch_labels = ("{{ project.package }}_{{ jtbd.id }}",)
depends_on = None


def upgrade() -> None:
	op.create_table(
		"{{ jtbd.table_name }}",
		sa.Column("id", sa.String(length=36), primary_key=True),
		sa.Column("tenant_id", sa.String(length=36), nullable=True),
		sa.Column("state", sa.String(length=64), nullable=False, server_default="{{ jtbd.initial_state }}"),
		sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
		sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
{% for field in jtbd.fields %}
		sa.Column("{{ field.id }}", sa.{{ field.sa_type }}, nullable={{ "False" if field.required else "True" }}),
{% endfor %}
	)
	op.create_index(
		"ix_{{ jtbd.table_name }}_tenant_state",
		"{{ jtbd.table_name }}",
		["tenant_id", "state"],
	)


def downgrade() -> None:
	op.drop_index("ix_{{ jtbd.table_name }}_tenant_state", table_name="{{ jtbd.table_name }}")
	op.drop_table("{{ jtbd.table_name }}")
