// Generated by flowforge JTBD generator — do not edit by hand.
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Vite config for the {{ project.name }} admin console.
//
// `build.outDir` is colocated with the package so the host can ship the
// admin app from `dist/` behind a separate ingress / auth proxy.
export default defineConfig({
	plugins: [react()],
	server: {
		port: 5174,
		strictPort: true,
	},
	build: {
		outDir: "dist",
		sourcemap: true,
		emptyOutDir: true,
	},
});
