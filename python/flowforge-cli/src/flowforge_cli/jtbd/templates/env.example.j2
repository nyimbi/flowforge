# {{ project.name }} — generated by flowforge JTBD generator
# Copy to .env and fill in real values before running.

DATABASE_URL=sqlite+pysqlite:///{{ project.package }}.db
TENANCY={{ project.tenancy }}
DEFAULT_LOCALE={{ project.languages[0] }}
DEFAULT_CURRENCY={{ project.currencies[0] }}

# Workflow definitions (JSON DSL) live alongside the backend.
FLOWFORGE_WORKFLOWS_DIR=workflows
